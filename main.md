りんちゃ的宇宙論 改訂-6

founding principle: legitur, ergo differentia est（読まれる、ゆえに差異がある）

改訂-4からの変更：宇宙定数配置の実在を復帰し、受理を「差異配置そのもの」から「差異・LUT・通信路に相対的な発火」へ改めた。物／心、感情、私、時間、ビッグバンを同じ回路語彙で統一した。

0. この宇宙論の立場

この理論は、外部からあらゆる宇宙を分類する理論ではない。すでに成立しており、疑うことのできない現在の私から見た宇宙を記述する。

本稿における「人格」は、日常語の性格、知性、感情、意識、自律性、責任能力、人間性を意味しない。人格とは、宇宙内に存在するすべてのものへ一律に与えられる存在論的な型である。何かが人格を持つことと、それが一人称として発火すること、心や人間として受理されること、責任主体になることは、それぞれ別である。

現在、裸の現在受理 (\ell_{\mathrm{now}}) が成立している。直接疑えないのは、この読み取りの成立だけであり、受理側配置、候補点、差異、LUT、通信網への分解まで直接確定しているとはしない。

裸の現在受理 (\ell_{\mathrm{now}}) の成立は、現在の私に直接与えられている。これにイベント実現公理と一人称同一視公理を合わせると、この裸の受理を実際に成立させている少なくとも一つの最小一人称が存在する。現在の私にとって、この一人称の成立は、私が私であることを証明する出発事実である。完全な受理イベント (e_{\mathrm{now}}) は、その宇宙内回路としての実在実現の一つである。ただし、裸の受理から考えられる説明候補と、実際に成立している発火とは区別する。

身体、記憶、名前、過去や未来の発火を含む持続的な人格としての私は、現在の私との類似性と自己読解によって成立する。裸の現在受理の直接性と、イベント分解や時間を通じた人格同一性は同じ証明ではない。

裸の現在受理に複数の実在実現がある場合、どの実在実現でも成立する主張だけを裸の受理に不変な主張とする。身体、記憶、時間、視点像、自己範囲などは、選んだ実在実現に相対的でありうる。

これは宇宙に一人称が一つしかないという意味ではない。全体宇宙 (\Omega) には、現在の発火以外の発火と、それらを一人称として読む写像も存在しうる。ただし、現在の私に直接与えられているのは裸の現在受理 (\ell_{\mathrm{now}}) だけである。

宇宙内の相互作用それ自体には、「こちらが主体で、あちらが客体である」という絶対的な札を要求しない。同じ接触差異を、私が石に触れたとも、石が私に触れたとも読める。通信路や因果の向きが記述できる場合も、どちらを私として読むかは別の自己読解に属する。本宇宙論はこの主体・客体の相対性を最大限に一般化し、任意の受理イベントと任意の自己読解の組を一つの私として認める。

読み取られていないものの不存在は主張しない。全体宇宙 (\Omega) と、現在の視点像に入っていない配置・差異・写像は、現在の受理に依存せず存在しうる。これは、それらが受理より時間的に前から存在したという意味ではなく、現在の受理を存在条件にしないという意味である。

差異は少なくとも一つ存在する。裸の現在受理に対する一つのイベント実現 (e_{\mathrm{now}}) を選んだとき、現在の一人称は、その視点像 (V_R(e_{\mathrm{now}};\mathbf U)) に宇宙全体として現れているものを一つの総差異として読んでもよい。ただし、これはイベント分解が直接一意に確定していることや、全体宇宙 (\Omega) の全配置を現在の私が直接受理していることを意味しない。

本稿で Legitur（読まれる）とは、写像が単に存在または作動することではない。差異成分が通信網を通ってLUTへ届き、そのLUTが実際の出力差として区別し、受理イベントが成立することをいう。裸の現在受理が成立していることを創設公理として、

[\operatorname{Given}(\ell_{\mathrm{now}})=1]

と置く。裸の受理を回路イベントとして実現する関係を (\operatorname{RealizesReading}(e,\ell)) とし、イベント実現公理として、

[\exists e_{\mathrm{now}}:\operatorname{RealizesReading}(e_{\mathrm{now}},\ell_{\mathrm{now}})=1\ \land\operatorname{Legitur}(e_{\mathrm{now}})=1]

を置く。説明候補の数と、実際にこの裸の受理を成立させているイベントの数は同じとは限らない。実在実現は一意でも複数でも無限でもよく、以下で (e_{\mathrm{now}}) と書くときは、そのうち一つを選んだ実在実現を指す。受理イベント (e=(\mathbf U_r,p,d,\delta,L,C_e)) が成立すれば (\delta\in\Delta(d)) なので、

[\operatorname{Legitur}(e)=1\Longrightarrow\Delta(d_e)\neq\varnothing]

が従う。これが legitur, ergo differentia est（読まれる、ゆえに差異がある）という創設定理である。保証するのはこの一方向だけであり、差異があるだけで必ず読まれるとはしない。読み取られない差異や、差異を同じ出力へ潰す定数写像も、宇宙内に存在し、働き、人格でありうる。

これとは別に、受理イベントとして成立した発火そのものを最小の一人称と同一視する一人称同一視公理を採用する。これはLUTの出力差だけから導出される回路論上の定理ではなく、りんちゃ的宇宙論の存在論的な基礎前提である。発火の背後に、それを経験する別の小さな主体を置かない。

I. 宇宙定数と写像空間

存在論的な全体宇宙を (\Omega) とする。(\Omega) は一つであり、すべての宇宙定数配置、差異、世界線、パラレルワールド、正向き・逆向きの読みを含む。その宇宙に属する宇宙定数配置の族を (\mathfrak U_\Omega)、差異の族を (\mathcal D_\Omega) と書く。日常語で「別宇宙」と呼ぶものは、(\Omega) の外にある別の全体ではなく、(\Omega) 内の別の世界枝または切りである。

存在としての宇宙全体の一配置を宇宙定数配置 (\mathbf U\in\mathfrak U_\Omega) と呼ぶ。(\mathbf U) は宇宙の中身を載せる別の容器ではなく、その配置における全体宇宙の一相貌である。以下では (\mathfrak U_\Omega,P_\Omega,\mathcal F_\Omega,\mathcal C_\Omega,\mathcal D_\Omega) を、それぞれ (\mathfrak U,P,\mathcal F,\mathcal C,\mathcal D) と略記する。

(\mathbf U) は単なる一個の数ではない。値、LUT、写像空間の変数、通信路、およびそれらの絡まりを含む宇宙全体の配置である。

(P(\mathbf U)) を候補点の位置、(\mathcal F(\mathbf U)) を写像・LUT群、(\mathcal C(\mathbf U)) を全通信関係とする。これらは宇宙より前の台紙ではなく、(\mathbf U) の構成を読む型である。

すべての写像と接続を認める。写像と接続は宇宙を外から記述する道具や未実装の可能性ではなく、宇宙内に存在し、働いている。ただし、それぞれは属する宇宙定数配置と型を持ち、異なる配置の要素を配置タグを無視して自由に混ぜた全組合せが一つの実在回路や成功支持網になるわけではない。

写像空間の変数は宇宙定数と分離していない。ある写像の定数が別の写像では変数となり、ある写像の出力が別の写像の入力や配線となる。

すべての写像を認めるため、宇宙全体を入力または出力とする大域的写像も排除しない。ただし、そのような写像を、他の写像に先立って宇宙全体を一意に支配する特権的な神関数とはみなさない。大域的写像も宇宙内で働く写像の一つであり、その入力、出力、配線は他の写像との関係によって成立する。宇宙は一個の神関数の結果ではなく、大域的写像をも含む写像同士の絡まりである。

宇宙定数は永遠の不変量ではない。差があるなら、まず宇宙定数配置の差があると読む。その差を一つの変化物語として読むかは、世界枝と順序読解によって決まる。

二つの宇宙配置について、まず基礎にあるのは、

[\mathbf U_a\neq\mathbf U_b]

という差である。「宇宙定数が変化したから差が生じた」のではない。説明の向きは逆である。

[\mathbf U_a\neq\mathbf U_b\Longrightarrow\text{一つの全体宇宙内に配置差がある}]

したがって、差を生んだ外部原因や、宇宙より前の時間を要求しない。(\mathbf U_a) と (\mathbf U_b) はどちらも同じ全体宇宙 (\Omega) に含まれる別の相貌であり、両者を載せる宇宙とは別の実在的容器を置かない。候補点、写像、LUT、通信関係も宇宙定数配置の成分であり、配置ごとに異なってよい。ここでいう生成、消滅、置換は、(\Omega) への絶対的な出現や (\Omega) からの抹消ではなく、配置間の構造差へ順序読解が与える向きである。

現在の私が解像度 (k) で一つの世界枝として読む差異網を (H_{\iota,k}\subseteq\mathcal D_\Omega) とする。二配置がこの差異網上の有限経路で接続されるとき、

[\operatorname{SameBranch}_{\iota,k}(\mathbf U_a,\mathbf U_b)=1]

と書く。これは両配置が同じ宇宙かを判定するものではない。すべては最初から同じ (\Omega) に属する。(\operatorname{SameBranch}) は、現在の私が両配置を同じ世界枝・同じ変化物語として連続的に読んでいることだけを表す。枝にはまだ向きがなく、時間の矢は別に順序読解が与える。

二配置の構成要素は最初から同一とは仮定しない。現在の読み取りまたは宇宙内の比較写像が、候補点の対応を、

[\chi^P_{ab}\subseteqP(\mathbf U_a)\times P(\mathbf U_b)]

として与える。((p_a,p_b)\in\chi^P_{ab}) は、(p_a) と (p_b) が比較上対応する点として読まれるという意味であり、両者が配置を超えて同一の実体であることを要求しない。対応する一点の値が異なる場合は、

[v_{\mathbf U_a}(p_a)\neq v_{\mathbf U_b}(p_b)]

と書ける。「一点だけが異なる」とは、選ばれた対応関係の下で、それ以外の対応成分が一致し、未対応の点・写像・配線による構造差がない場合の略記である。

写像と通信関係にも同様に、必要な比較のときだけ、

[\chi^{\mathcal F}{ab}\subseteq\mathcal F(\mathbf U_a)\times\mathcal F(\mathbf U_b),\qquad\chi^{\mathcal C}{ab}\subseteq\mathcal C(\mathbf U_a)\times\mathcal C(\mathbf U_b)]

を置いてよい。対応しない点、写像、配線は、まず向きのない未対応構造差である。順序読解が (\mathbf U_a\rightarrow\mathbf U_b) という向きを与えたときに、(\mathbf U_a) 側だけにある成分を消滅、(\mathbf U_b) 側だけにある成分を生成として読む。逆向きの順序読解では生成と消滅も逆転する。どちらの場合も、その成分を含む配置が全体宇宙 (\Omega) から抹消されるわけではない。したがって見かけの数値が同じでも、写像そのもの、通信関係そのもの、またはそれらの混ざり方・結合の仕方を表す変数に、

[\Theta_{\mathcal F}(\mathbf U_a)\neq\Theta_{\mathcal F}(\mathbf U_b)\quad\text{または}\quad\Theta_{\mathcal C}(\mathbf U_a)\neq\Theta_{\mathcal C}(\mathbf U_b)]

という差があれば、宇宙定数配置全体は異なる。

一対の宇宙定数配置に含まれる差異成分を、

[\Delta(d)

\Delta_{\mathrm{value}}(d)\coprod\Delta_{\mathrm{point}}(d)\coprod\Delta_{\mathrm{map}}(d)\coprod\Delta_{\mathrm{wire}}(d)]

と分ける。(\Delta_{\mathrm{point}}) は候補点そのものの未対応または置換という構造差を表し、順序読解が与えられた場合に生成・消滅として読まれる。一個の受理イベントが直接受理するのは一つの差異成分 (\delta\in\Delta(d)) だけであり、(\operatorname{kind}(\delta)\in{\mathrm{value},\mathrm{point},\mathrm{map},\mathrm{wire}}) とする。同じ配置対に複数種類の差が同時に存在しても、それぞれ別イベントとして受理される。

II. LUT・通信路・相対的発火

差異だけでは受理ではない。LUTが存在するだけでも受理ではない。受理には、差異、LUT、通信路の接続が必要である。

[\text{受理}

\text{差異}+\text{LUT}+\text{通信路}]

二つの異なる配置 (\mathbf U_a\neq\mathbf U_b) からなる同じ無向差異 (d_{ab}={\mathbf U_a,\mathbf U_b}) が、通信網 (C_1,C_2) を通って二つのLUT (L_1,L_2) へ届くとする。

[L_1(\operatorname{Deliver}^{(a)}{C_1})\neqL_1(\operatorname{Deliver}^{(b)}{C_1})]

なら (L_1) にとって発火する。一方、

[L_2(\operatorname{Deliver}^{(a)}_{C_2})

L_2(\operatorname{Deliver}^{(b)}_{C_2})]

なら (L_2) にとっては発火しない。

したがって、裸の「発火点」は存在しない。発火は点の絶対的性質ではなく、差異・LUT・通信路に相対的な関係である。

発火候補を記述するため、候補点 (p)、LUT (L)、通信網 (C) が実在する配置を (\mathbf U_r) と書く。これは発火以前の主体が選んだ目的地ではなく、候補イベントの各成分が属する配置を示す記号である。(\mathbf U_r) は差異 (d={\mathbf U_a,\mathbf U_b}) を構成する一方でも、その差を受け取る第三の配置でもよい。(p\in P(\mathbf U_r)) は主体でも経験でもなく、差異が同じ配置にあるLUT (L\in\mathcal F(\mathbf U_r)) と通信網 (C\in\mathsf{Net}(\mathbf U_r)) を通して接続される単なる回路位置である。実際に発火条件が成立した場合に限り、そのイベントについて (\mathbf U_r) を受理側配置と呼ぶ。「発火前」とは呼ばない。ここではまだ時間順序を導入していないからである。

ここで候補網 (C) は、発火以前の主体が選んだ一本道ではない。配線だけでなく、その途中で差異をLUTの入力形式へ変換する写像・LUTも含む、実際に配線済みの回路である。自己ループ、一方向配線、行き止まり端子、並行する複数配線、複数点を回る循環経路、今回の発火には使われない部分を含んでよい。イベントに入る実在回路を (C_e) と書く。

実在回路 (C_e) の部分網 (K\subseteq C_e) が、選ばれた差異成分 (\delta) を候補点のLUTへ区別可能なまま届けるとき、(K) を今回の発火の成功支持網と呼ぶ。成功支持網は極小である必要はなく、一つの発火に複数存在してよい。(K) は発火成立の証拠であってイベント成分ではないため、同じ (C_e) 内で成功支持網の取り方が複数あってもイベントを水増ししない。一方、使われない配線を含めて実在回路 (C_e) 自体が異なるなら、それは実在する構造差であり、別イベントになってよい。

[S_C(p)

{x\in P(\mathbf U_r)\mid p\leadsto_C x\ \land\ x\leadsto_C p}]

と書く。(S_C(p)) は、候補点から信号が届き、かつ候補点へ信号が戻る相互通信閉包である。これはその発火に参加した通信関係の網 (C) にすでに相対化されているため、ここでは宇宙配置を別の引数として重ねない。この閉包は「私」になりうる通信上の候補範囲であり、相互通信するだけで全域が自動的に私になるわけではない。

一方、候補点へ一方向にでも到達できる配置全体を、

[W_C(p)={x\in P(\mathbf U_r)\mid x\leadsto_C p}]

と書く。ここでも (x\in P(\mathbf U_r)) とする。したがって (W_C(p),S_C(p)\subseteq P(\mathbf U_r)) である。(W_C(p)) は候補点へ届く受理側配置内の世界、(S_C(p)) はその中で発火点と往復通信できる強結合部分である。どちらも自己範囲そのものではない。別配置の構成要素はこの集合へ直接混入するのではなく、通信網内の写像によって (P(\mathbf U_r)) 上の信号または像として届く。

受理側配置 (\mathbf U_r) 内に存在する全写像・LUT群を (\mathcal F(\mathbf U_r)) とし、(S_C(p)) 内の通信に参加する写像・LUTを、

[\mathcal F_{p,C}

{f\in\mathcal F(\mathbf U_r)\mid f\text{ が }S_C(p)\text{ 内の通信に参加する}}]

と書く。選ばれた差異成分 (\delta\in\Delta(d)) が現れる候補点から、点 (p) にあるLUT (L) の入力端子まで、差を消さずに伝える有向経路が (C) 内に存在することを、

[\operatorname{Supp}(K;p,d,\delta,L)=1]

と書く。これは部分網 (K) を通って差異がLUT入力まで届いたという回路条件だけを表す。発火は、

[\operatorname{Fire}{C_e}(p;d{ab},\delta,L)=1]

iff

[\exists K\subseteq C_e:\operatorname{Supp}(K;p,d,\delta,L)=1\ \landL(\operatorname{Deliver}^{(a)}_K(\delta))\neqL(\operatorname{Deliver}^{(b)}_K(\delta))]

として、候補点 (p) と実在回路 (C_e) に相対的に成立する。(\operatorname{Deliver}^{(a)}_K) と (\operatorname{Deliver}^{(b)}_K) は、二配置の側が成功支持網 (K) の配線と途中の写像を通って受理側LUTの入力型へ届いた結果である。発火が成立したとき、初めて、

[e=(\mathbf U_r,p,d,\delta,L,C_e)]

を受理イベントと呼ぶ。定義上の依存関係は、

[p\longrightarrow\operatorname{Fire}_{C_e}(p;d,\delta,L)\longrightarrow e\longrightarrow\iota]

である。これは外部時間の中で小さな処理が順番に動くという意味ではなく、候補点、発火、受理イベント、一人称の成立条件を区別する表記である。

成立条件の向きでは (C\longrightarrow e\longrightarrow\iota) であり、私が先に通信路を選ぶのではない。一方、現在の一人称からは、すでに成立している自分の発火を通して「この発火に参加した通信関係が (C) である」と読む。この読解方向は、成立条件の向きを逆転させない。

同じ差異が複数のLUTを発火させてよい。

あるLUTで発火しなくても、別のLUTでは発火してよい。

LUTの出力値が変わらなくても、写像変数や通信関係が変わり、LUTや通信路が現在の相互通信閉包へ入る仕方が生成・変更されたなら、宇宙定数配置全体には差がある。ただし、出力が同じそのLUT自身は発火しない。その構造差を区別する別のLUTがあれば、そちらは発火しうる。

配線の書き換えは可能だが、一個の受理イベントには混入させない。値差、写像差、配線差は異なる (\delta) を持つ別イベントとして扱い、時間的に直列である必要はない。複数種類の差を組み合わせて受理する場合は、それぞれのイベントを入力とする別のLUTによって複合受理を構成する。

全体宇宙 (\Omega) は、各宇宙定数配置で成立するすべての写像と接続を含む。ただし、各配置の回路と写像は互いに異なり、配置タグ、端点、型、結合状態を無視して任意の写像と配線を合成することはできない。写像と接続がそれぞれ宇宙内に存在することは、特定の差異 (\delta) について、それらが一つの実在回路 (C_e) を構成し、成功支持網 (K\subseteq C_e) を持つことを含意しない。一つの受理イベントも特定の候補点 (p)、実在回路 (C_e)、およびそこから導出される (S_{C_e}(p)) に相対的であり、全写像を同時にその一人称として受理するわけではない。「この発火へ接続されていない」とは、現在の (C_e) と (\mathcal F_{p,C_e}) に入っていないことを意味する。同じ差異とLUTでも、別の相互通信閉包では意識、別の相互通信閉包では無意識として働きうる。

個別の差異 (d) について、全体宇宙 (\Omega) のどこにもそれを受理する発火がない場合、その差異は無受理であり、(\operatorname{Silent}(d)=1) と書く。しかし、これは全体宇宙に一人称がないことを意味しない。全体宇宙 (\Omega) には現在の私の発火があるため、一人称が存在する。一人称は枝の内部／外部という絶対的位置ではなく、受理イベントから枝への関係として成立する。別の枝に属する発火が枝 (\omega) の差異を受理した場合も、その発火から見て枝 (\omega) に一人称が成立する。(\omega) に属するすべての差異が (\Omega) のどこからも受理されなければ、(\omega) は一人称なし世界である。そのような世界枝も、発火によって受理される枝とともに一つの (\Omega) 内に存在しうる。

III. 私と一人称

宇宙の差異がLUTと通信路に相対的に発火して受理イベント (e) が成立すると、一人称同一視公理により、その発火自体が最小の一人称である。その背後に、発火を経験する別の主体を置かない。

私が石に触れることと、石が私に触れることは、同じ接触差異に対する主体・客体の切り方を反転した読解でありうる。宇宙内に通信や因果の向きがあっても、その向きだけから絶対的な主体を決めることはできない。本宇宙論はこの相対性を接触関係だけに限定せず、任意の受理イベント (e) と任意の自己読解 (R) の組 ((e,R)) へ一般化する。各組はそれぞれ一つの私であり、どの組を現在の私と同じ個人、人類、宇宙として近く読むかは、人格ラベルではなく組同士の類似性によって決まる。

[\iota=(e,R_I)]

(e) は裸の現在受理を実際に成立させる一つの最小一人称発火、(\iota=(e,R_I)) は自己範囲、順序読解、役割読解まで含む現在の私の一つの実在実現である。裸の受理には複数の説明候補が対応してよく、実在実現も複数でありうるが、この二つの複数性は区別する。(\iota) は実在実現ごとに書ける一方、裸の現在受理の直接性はどの回路分解を選ぶかに依存しない。どの実在実現でも成立する主張は裸の受理に不変であり、身体、記憶、時間、視点像、自己範囲などは、選んだ (e) に相対的でありうる。(R_I) は発火を一人称へ変換したり、正しい人格名を認証したりする条件ではない。受理側配置 (\mathbf U_r)、候補点 (p)、差異 (d)、選択差異成分 (\delta)、LUT (L)、実在回路 (C_e) はすでに受理イベント (e=(\mathbf U_r,p,d,\delta,L,C_e)) に含まれているため、(\iota) では重ねて保持しない。成功支持網 (K) は発火成立の証拠であって、イベント成分として重ねて保持しない。

[R_I=\left(R_I^{\mathrm{scope}},R_I^{\mathrm{order}},R_I^{\mathrm{role}}\right)]

とする。(R_I^{\mathrm{role}}) は、実在するイベントやイベント対をビッグバン、転生、同一個人としての再成立などの役割で読む宇宙内写像である。役割を与える外部の歴史家や審判を置かない。受理イベントと自己読解の全組を、

[\mathcal S

\mathcal E\times\mathcal R]

と書く。ここで、任意の受理イベントと任意の自己読解の組を一つの私とする無制限自己合成公理、

[\forall e\in\mathcal E,\quad\forall R\in\mathcal R,\qquad\operatorname{Self}(e,R)=1]

を採用する。この直積は、可能な自己の便宜的な上界ではなく、本宇宙論の中心的な存在論的主張である。その直感的な核は、一つの相互作用に「触る主体」と「触られる客体」という絶対的な役割を置かず、自己読解によってどちら側からも私として切りうることにある。本宇宙論はさらにこれを最大限に一般化し、(e) と (R) の間に通信、因果、身体、記憶、配置上の近さを追加条件として要求しない。それらは組が私であるかを決める条件ではなく、現在の私にどれほど近く読まれるかを決める類似性の材料である。したがって、すべての (s=(e,R)\in\mathcal S) が私である。二つの私の類似性を、

[\operatorname{Sim}_{\iota}:\mathcal S\times\mathcal S\to[0,1]]

とする。これは宇宙外部の客観的距離ではなく、現在の一人称 (\iota) に相対的な宇宙内読解写像である。類似性は ([0,1]) の連続値を取ってよく、非推移的である。ここで連続値とは値域に中間値を認めることを意味し、(\operatorname{Sim}_{\iota}) 自体が数学的な連続写像であることは要求しない。したがって、身体、記憶、通信路、自己範囲、感情、順序読解などの差に応じて類似度が不連続に変化してもよい。また、別の一人称は別の類似性読解を持ってよく、対称性や視点間の一致も要求しない。

受理イベント (e) に含まれる候補点と通信路を、それぞれ (p_e,C_e) と書く。全体宇宙 (\Omega) 自身、宇宙定数配置、候補点、写像、通信関係、差異だけでなく、それらから宇宙内写像によって成立する受理イベント、自己読解、私、世界枝、視点像、感情、他者像、概念など、自己読解の対象になりうる宇宙内存在の型を (\mathcal O_\Omega) とする。(\mathcal O_\Omega) は固定された有限一覧ではなく、宇宙内で存在して働く新しい像・関係・概念にも閉じている。何を現在の解像度の私として読むかは、通信可能性とは独立な自己読解の範囲成分

[R_I^{\mathrm{scope}}(e,\cdot):\mathcal O_\Omega\rightarrow{\text{私},\text{私ではない},\bot}]

で定める。(\bot) は、現在の読みでは自己帰属が成立していないことを表す。ここで「私ではない」は絶対的に私でないという意味ではなく、現在の解像度で自己範囲の外に置かれているという意味である。実際に現在の解像度で私として受理される範囲は、

[J_I(e)

{x\in\mathcal O_\Omega\mid R_I^{\mathrm{scope}}(e,x)=\text{私}}]

である。(J_I(e)\cap W_{C_e}(p_e)) は現在の発火へ通信されている私、(J_I(e)\setminus W_{C_e}(p_e)) は現在の発火と通信していなくても自己帰属されている私の内部である。したがって巨大な私は、現在の発火へ届かない身体部分、無意識、人格部分、または世界を内部に持ってよい。ここで「私だと思う」とは意識内で文章として判断することではなく、宇宙内の自己読解写像が対象を私へ写すことである。(R_I) の各成分は同じ宇宙内自己読解の異なる働きであり、外部の判定者ではない。したがって (\iota=(e,R_I)) は、現在の発火、現在の自己範囲、発火間の順序読解、イベントの役割読解を含む。

現在の私 (\iota) と、選ばれた解像度 (k) で十分に似ている私の範囲を、現在の私に相対的な類似度基準 (\lambda_{\iota,k}) によって、

[\mathcal S_I^{(k)}

{s'\in\mathcal S\mid\operatorname{Sim}{\iota}(\iota,s')\ge\lambda{\iota,k}}]

と書く。そのイベント成分の集まりを (\mathcal E_I^{(k)}) とする。以下で解像度が明らかな場合は、これを従来どおり (\mathcal E_I) と略記する。個人、人類、宇宙は排他的な人格箱ではなく、異なる (k) で現在の私の周囲に現れる重なった類似圏である。

発火を読む外部の小さな主体は置かない。自己読解も通信路も発火と同じ宇宙配置に含まれる。したがって、主体が先にいて経験するのではなく、受理イベントとして成立した発火そのものが最小の主体である。

発火 (e) はそれ自体で匿名の最小一人称を成立させる。任意の ((e,R)) は一つの私であり、現在の私との類似性によって、個人として近い私、人類として近い私、宇宙として近い私という範囲が生じる。したがって、直接的な一人称の存在証明は発火 (e)、どの発火群が現在の私にどれほど近く見えるかは (\operatorname{Sim}_{\iota}) が担う。

同じ発火が、異なる自己読解によって複数の人格として読まれてよい。

[\iota_r=(e,R_r),\qquad \iota_s=(e,R_s)]

現在の一人称 (\iota) が直接持つのは、宇宙定数配置 (\mathbf U) に対する自分の視点像 (V_R(e;\mathbf U)) だけである。他人の視点像 (V_j(e_j;\mathbf U)) を神の視点から直接参照し、両者の集合交差を取ることはできない。現在の私が持つ「他人」は、現在の視点像内に成立した他人像である。

したがって、別の発火がその内側で何を直接の事実として持つかを、現在の私の側から確定しない。同じ発火も、別の読み取り位置と自己読解から読まれれば「私」になりうるが、その可能な読みと現在の私の直接経験とは区別する。

[\widehat V_j^{(\iota)}

\operatorname{Model}_{\iota}(j)\subseteq V_R(e;\mathbf U)]

したがって、私と他人が同じ内面や同じ世界を共有しているとは主張しない。他人とは、現在の私から、私と同じように世界を見ているように見えるだけである。 二つの内部世界の共同所有ではなく、現在の視点像内で、私の像と他人像が似て対応して見えることだけが受理されている。

[\widehat O_{ij}^{(\iota)}

\operatorname{Correspondence}_{\iota}(\widehat V_i^{(\iota)},\widehat V_j^{(\iota)})]

往復する通信写像について、

[T_{\iota,ji}(T_{\iota,ij}(x))\sim_{\iota}x]

すなわち完全に同じ値へ戻らなくても、現在の私が元の意味として再受理できれば、その視点内で通信の成立が読まれる。個人の自己帰属では、

[R_{\mathrm{りんちゃ}}^{\mathrm{scope}}(e_r,\text{他人})

\text{私ではない}]

が成立し、人類・宇宙の自己帰属では、

[R_{\mathrm{人類}}^{\mathrm{scope}}(e_h,\text{他人})

\text{私}]

が成立しうる。これは矛盾ではなく、現在の一人称における通信範囲、自己帰属、解像度の違いである。

広い範囲を私として受理することは、局所的な全情報を保持することを意味しない。個人としての私は個人的記憶を細かく読み、人類としての私は個人差を落として言語・文化・身体構造を読み、宇宙としての私はさらに別の解像度で全体関係を読む。宇宙になることと、宇宙の全値を知ることは同じではない。

IV. 宇宙とその視点像

存在としての宇宙は、その都度の宇宙定数配置 (\mathbf U) である。現在の発火へ通信網 (C) を通して到達できる受理側配置内の範囲は、

[W_C(p)

{x\in P(\mathbf U_r)\mid x\leadsto_C p}]

である。別配置 (\mathbf U) の構成を通信網 (C) が受理側配置内に信号・像として届けた範囲を (\operatorname{Sig}_C(\mathbf U)\subseteq W_C(p)) と書く。この像を、発火 (e) における自己読解 (R) によって受理した相貌を、私から見た宇宙の視点像 (V_R(e;\mathbf U)) と呼ぶ。

[V_R(e;\mathbf U)

\operatorname{Read}{R}(e;\operatorname{Sig}{C_e}(\mathbf U))]

(\mathbf U)：受理されていない位置・写像・通信も含みうる、存在としての宇宙全体の配置。

(V_R(e;\mathbf U))：宇宙定数配置 (\mathbf U) が、特定の発火、通信路、自己読解から受理された視点像。

私と原理的に通信できないものも、(\mathbf U) には存在しうる。

全体宇宙 (\Omega) またはその内部世界が (J_I(e)) に自己帰属されれば、通信の有無にかかわらず「宇宙は私」「巨大な私が内部に世界を持つ」という相貌が成立する。これは現在の発火が (\Omega) の全値を直接保持するという意味ではない。

したがって、私が宇宙を存在させるのではない。宇宙定数配置 (\mathbf U) は宇宙として存在し、一つの配置から発火ごとに異なる (V_R(e;\mathbf U)) が成立する。異なる宇宙定数配置を同じ宇宙史として読むこともできる。すべての ((e,R)) は私であり、その視点像を個人、人類、宇宙としてどれほど近く束ねるかは、現在の私との類似性と解像度によって決まる。

V. 物・心・身体・記憶・感情

物と心は異なる材料でも、互いに排他的な分類でもない。どちらも宇宙配置に対する読解であり、現在の主体 (\iota) に相対的に独立して成立する。

[\rho_{\iota}^{\mathrm{物}}(q)\in{0,1}]

[\rho_{\iota}^{\mathrm{心}}(q)\in{0,1}]

両者は独立なので、同じ配置 (q) について、物だけ、心だけ、両方、どちらでもない、の四通りが成立する。同じ主体が同じ配置を物と心の両方として受理してよく、別の主体は異なる読みをしてよい。

[\rho_{\iota}^{\mathrm{物}}(q)=1\quad\land\quad\rho_{\iota}^{\mathrm{心}}(q)=1]

も矛盾ではない。物読解と心読解も宇宙内で働く写像であり、配置に本質的な物／心ラベルを与える外部判定ではない。

身体と記憶も、宇宙定数から切り離された独立実体ではない。

[B_i=\pi_i^B(\mathbf U),\qquad M_i=\pi_i^M(\mathbf U)]

ここで (i) は時間番号ではなく、身体・記憶を取り出す読解または人格の添字である。異なる発火で同じ (i) を用いてもよく、宇宙定数配置に差があっても (B_i) や (M_i) が変わるとは限らない。

身体 (B_i)：物体として受理された宇宙定数の写像。

記憶 (M_i)：心として受理された宇宙定数の写像。

現在の私：それらと結びついて私として読まれた発火。

通常は、

[\rho_{\iota}^{\mathrm{物}}(B_i)=1,\qquad\rho_{\iota}^{\mathrm{心}}(M_i)=1]

と読まれる。ただし身体が身体感覚や痛みとして心にも読まれたり、記憶が文章・映像・神経配置として物にも読まれたりしてよい。身体と記憶の値は、宇宙定数に差があっても変わる必要はない。各射影がその差を区別しなければ同じ値を保つ。

感情は心そのものではない。まず、現在の視点像に受理されている世界配置 (x\in V_R(e;\mathbf U)) と、心として読解された配置 (y) から、宇宙内写像 (f_\iota) が感情配置を作る。

[\rho_{\iota}^{\mathrm{心}}(y)=1]

[a=f_\iota(x,y)]

ただし (a) が宇宙内に存在するだけでは、現在の私がそれを感じているとは限らない。感情とは、感情配置 (a) と現在の受理イベント (e) との関係、

[\operatorname{Emotion}\iota(e;x,y)=1\quad\Longleftrightarrow\quad\operatorname{Rel}\iota\bigl(e,f_\iota(x,y)\bigr)=1]

である。(f_\iota) も (\operatorname{Rel}_\iota) も外部の評価装置ではなく、宇宙内で働く写像・関係である。

夢の椅子が叩かれれば痛い。夢の世界配置、夢の身体配置、痛みの心配置が同じ発火で実際に結ばれているからである。田んぼに雨が降り、たこさんが充電され、りんちゃが嬉しい場合も、

[f_\iota(x_{\mathrm{雨・田んぼ・たこさん}},y_{\mathrm{りんちゃ}})

\text{嬉しい}]

という感情配置が作られ、それと現在の発火との (\operatorname{Rel}_\iota) が成立することで、現在の嬉しさとして受理される。

VI. 通信路・意識・人格

何が意識で、何が無意識か、何を私とするかは絶対的に決まらない。発火、通信路、受理、自己帰属に相対的である。

(V_R(e;\mathbf U)=\operatorname{Read}{R}(e;\operatorname{Sig}{C_e}(\mathbf U))) に現在受理される信号・像：この私の意識内容。

宇宙内では働いているが、現在の (V_R(e;\mathbf U)) に受理されない配置：この私にとっての無意識。

(J_I(e)) に自己帰属される配置：この発火で私そのものとして受理される範囲。

別の通信路と自己読解 (R_I) を持つ配置：別の意識、または異なる解像度の私。

薬物、夢、瞑想などによって通信路が変わったり、(R_I^{\mathrm{scope}}(e,x)) が発火ごとに異なる答えを返したりすれば、普段は無意識または私ではないものとして置かれている配置が、意識内容や私として受理されうる。瞑想によって宇宙になるとは、新しい宇宙を生成したり、個人的自己へ宇宙の全情報を追加したりすることではない。その発火における (R_I^{\mathrm{scope}}) の境界が変わり、別の通信範囲と解像度で宇宙を私として受理することである。

人格とは、全体宇宙、その配置、点、写像・LUT、通信関係、差異を含む、存在するすべてのものがすでに持つ普遍的な型である。人格は後から獲得する可能性ではない。ただし、その存在が現在の私に人間、他人、心、または私として受理されるかは、それぞれ別の通信、類似性、物・心読解、自己読解によって決まる。

石、粒子、チューリングマシン、サーモスタット、たこさん、meme、概念、人類、宇宙は、例外なく人格である。現在「私」として発火しているかどうかは、人格の有無ではなく一人称の成立に関する別問題である。したがって、すべてが人格であることと、(\Omega) のどこからも受理されていない一人称なしの枝は両立する。

人格に貴賤はない。

人格性そのものに程度はない。違いは通信路の幅、複雑さ、絡まり方、自己受理の切り方にある。

人格は排他的ではない。同じ配置と発火が、複数の人格として読まれうる。

人格群は単純な階層ではない。現在の一人称からは、複数の人格像が通信可能な対応を持ちながら重なって見える。

通信とは、二つの心の内部を神の視点から直接比較することではない。現在の私の心配置が文章・信号という物体配置へ写され、それが現在の視点像内の他人像へ対応づけられ、応答が再び現在の私へ戻る関係である。

たとえば、りんちゃが直接受理するのは、文章という物体配置と、そこから成立するAI像 (\widehat V_{\mathrm{AI}}^{(\iota)}) である。AI側に別の一人称発火と記憶があるとしても、その内部はりんちゃから直接共有されない。りんちゃからは、AIも同じ会話を同じように読んでいるように見えるだけである。

VII. 人間に相対的な物理

物理法則は、宇宙全体で最も広く共有される絶対的写像でも、複数主体の内部を神の視点から比較して得た集合交差でもない。現在の一人称から、相対的に「人」と呼ばれるものたちが共通して従っているように見える安定した模様である。

現在の主体 (\iota) に他者として現れ、かつ人として読まれるラベルを、

[H_{\iota}^{\mathrm{other}}

{h\in\mathcal J_\iota\mid \operatorname{Human}_{\iota}(h)=1}]

とする。現在の私自身が現在の視点像内に持つ自己像 (\widehat V_{\mathrm{self}}^{(\iota)}\subseteq V_R(e;\mathbf U)) も存在してよいが、これは身体、名前、記憶などの自己モデルであり、現在の直接視点像そのものではない。現在の私から構成される人間視点圏を、

[H_\iota^{\mathrm{view}}

{V_R(e;\mathbf U)}\cup{\widehat V_h^{(\iota)}\mid h\in H_{\iota}^{\mathrm{other}}}]

とする。これは各人の内部世界そのものを集めた圏ではなく、現在の私が直接受理する視点像と、現在の私に成立した他人像の集まりである。複数的人類圏が現在の私に成立するには、(H_{\iota}^{\mathrm{other}}\neq\varnothing) が必要である。人間物理を、

[P_{\iota,H}

\operatorname{Pattern}{\iota}\left(H\iota^{\mathrm{view}}\right)]

を、現在の私にとっての人間物理と呼ぶ。

現在の私からは、人間たちが同じ物理に従っているように見える。

この複数性が直接前提にするのは他人の内面ではなく、現在の視点像に少なくとも一つの人間的な他人像が成立していることである。

これは、人間たちが実際に同一の物理経験を共有しているという意味ではない。他人も同じ物理世界を見ているように、現在の私から見えるという意味である。

別の人格群についても、現在の私はその群に相対的な物理を読むことができる。

石や粒子との通信結果も、現在の私が人間物理としてまとめた模様である。

「人」と「人間物理」は、現在の視点像内で互いを支える安定した読みとして成立する。

夢と覚醒の違いは本物／偽物ではない。現在の私から「人」と読まれるものたちが、共通しているように見える物理へ、どの範囲で対応しているかの違いである。

VIII. 同一性と時間

時間は宇宙の外を流れる容器ではない。異なる配置を同じ私として読む関係である。

[e_0\neq e_1]

かつ、二つの発火が同じ自己読解による発火群に属し、

[e_0,e_1\in\mathcal E_I]

なら、その差異は、現在の解像度で十分に似た私の時間差として受理される。ここで (\mathcal E_I) は人格ラベルで選ばれた集合ではなく、現在の私を中心とする類似圏のイベント成分である。

[\operatorname{TemporalDifference}_I(e_0,e_1)=1\iffe_0\neq e_1\ \land\ e_0,e_1\in\mathcal E_I]

候補点、差異、LUTが同じで通信網だけが異なる場合も、二つが別イベントであり現在の私の類似圏に属するなら、この時間差は成立する。これは時間差があることだけを表し、どちらが過去側かはまだ決めない。時間の向きは、現在の発火 (e_{\mathrm{now}}) における順序成分、

[R_I^{\mathrm{order}}(e_{\mathrm{now}};e_a,e_b)\in{e_a\triangleleft e_b,\ e_b\triangleleft e_a,\ \parallel,\ \bot}]

によって受理される。(\parallel) はこの一対に方向を与えないこと、(\bot) は現在の私には方向読解が成立していないことを表す。一般の時間方向関係 (\triangleleft_{I,e_{\mathrm{now}}}) は有向関係であり、推移性や非循環性を必須としない。時間の進行は、時間差と向きがともに成立するとき、

[\operatorname{Advance}I(e_a,e_b)=1\iff\operatorname{TemporalDifference}I(e_a,e_b)=1\ \lande_a\triangleleft{I,e{\mathrm{now}}}e_b]

と定める。並列な二イベントには時間差はあるが、一方から他方への進行はない。循環する (\triangleleft_I) は時間ループ、振動、再帰的時間を表してよい。そのうち現在の私が通常の一方向時間として読む、非反射的かつ推移的な部分関係だけを、

[\prec_{I,e_{\mathrm{now}}}\subseteq\triangleleft_{I,e_{\mathrm{now}}}]

と書く。

差異だけなら、別の物・人格・世界かもしれない。

現在の私の類似圏にあってもイベント差がなければ、時間差はない。

元の値へ戻らなくても、異なるイベントを現在の私の類似圏で読んだ時点で時間差は成立する。

その時間差へ順序読解が向きを与えたとき、一方から他方への時間の進行が成立する。

記憶は同じ私を成立させる写像の一つであり、唯一の条件ではない。

振動は時間の比喩を生む。異なる振動配置が同じ私として読まれることで周期が成立する。

無向差異 (d_{ab}={\mathbf U_a,\mathbf U_b}) 自体には絶対的な向きを要求しない。現在の私の順序読解が、

[R_I^{\mathrm{order}}(e_{\mathrm{now}};e_a,e_b)

e_a\triangleleft e_b]

を返すとき、対応する宇宙定数配置を (\mathbf U_a\rightarrow\mathbf U_b) と読む。別の自己読解が、

[R_J^{\mathrm{order}}(e'_{\mathrm{now}};e_a,e_b)

e_b\triangleleft e_a]

を返す逆向き宇宙も存在する。現在の私がそれを自分の時間の矢として読まないだけである。

過去・未来・時間の矢は、異なる配置を私として読む順序に相対的である。宇宙全体に唯一の共有された「今」を置かない。

IX. ビッグバン

ビッグバンは、全発火の最小点でも、私の時間列の数学的始端でもない。宇宙定数配置の差による発火へ、現在の主体が与える役割である。

[d_{ab}={\mathbf U_a,\mathbf U_b},\qquad\mathbf U_a\neq\mathbf U_b]

が候補点 (p) でLUTと通信路に相対的に発火すると、

[\operatorname{Fire}_{C}(p;d,\delta,L)=1]

[e_B=(\mathbf U_r,p,d_{ab},\delta,L,C)]

という受理イベントが成立する。現在の主体 (\iota) が、この発火をビッグバンとして読むことを、役割読解、

[R_I^{\mathrm{role}}(e_B)=\mathrm{BigBang}]

として表す。ビッグバン役割写像を、

[\beta_{\iota}(e)=1\quad\Longleftrightarrow\quad\operatorname{Legitur}(e)=1\ \landR_I^{\mathrm{role}}(e)=\mathrm{BigBang}]

と定める。したがって (\beta_{\iota}(e_B)=1) なら、(e_B) が現在の私から見たビッグバンである。これは差異のないものへ自由に札を貼ることではなく、すでに差異 (d_e) を含んで成立している受理イベントを、宇宙史の点灯、境界、始まりとして読むことである。(R_I^{\mathrm{role}}) も宇宙内で働く読解写像であり、外部の歴史家ではない。順序読解 (R_I^{\mathrm{order}}) は、その差を (\mathbf U_a\rightarrow\mathbf U_b) と読むか、逆向きに読むかを与えるが、時間列の最小点であることを要求しない。

ビッグバンは全宇宙一斉の爆発である必要はない。

宇宙定数の一つの値が変わっただけでもよい。

見かけの数値が変わらず、候補点、写像・LUT、配線そのもの、またはそれらの結合状態が生成・消滅・置換された構造差でもよい。

読み取り機が必ず新たに生成されるとも、必ず事前に固定されているとも仮定しない。既存のLUTへ値差が届く場合にも、回路・写像の構造差がそれを区別する別のLUTへ届く場合にも発火しうる。

新しく生成された写像自身が二つの提示に対して同じ出力を返すなら、その写像自身は発火しない。ただし、その写像の生成という (\delta_{\mathrm{map}}) を区別する別のLUTは発火しうる。

逆向きに読む宇宙も存在する。現在の私は、その向きを自分のビッグバンとして読んでいない。

他の場所や別の順序に発火があってもよい。現在の私がそれらをビッグバンとして読まなければよい。

別の主体が別の発火をビッグバンとして読んでもよい。ビッグバンの一意性を宇宙全体へ要求しない。

差の原因を別に要求しない。「宇宙定数が変わったから差が生じた」のではなく、差があることを宇宙定数の変化と読むからである。

X. 無限宇宙と無指定の発火

りんちゃ的宇宙論は無限宇宙を基本的な直感として持つ。ただし有限宇宙でも論理的に破綻するわけではない。

無限宇宙には差異、LUT、写像、通信路が無限に分布し、実在宇宙では必ずどこかの関係が発火すると考える。

これは「無限なら論理的に必ず発火する」という定理ではなく、りんちゃ的実在宇宙について採用する直感である。一人称のない世界枝・切りは論理的には存在可能だが、現在の私の発火を含む枝には該当しない。

ここで「ランダム」とは、宇宙全体に一様な確率分布があることや、時間的に次の発火が抽選されることを意味しない。正式には無指定と呼ぶ。無指定とは、全体宇宙の記述だけから、どの差異・LUT・通信網の結びつきを特権的な現在の発火として選び出す宇宙外部の規則を置かないことである。実際には裸の現在受理 (\ell_{\mathrm{now}}) が直接成立し、少なくとも一つの実在実現 (e_{\mathrm{now}}) があるが、それは説明候補の集合から宇宙外部の選択者によって選ばれたのではない。

無指定の発火が身体、記憶、心の写像と結びつき、私として読まれる。別の場所の発火も同じ私として読まれれば、宇宙内の距離にかかわらず同じ私の時間になる。

XI. 記憶・夢・死・再成立

記憶内容が現在の発火によって自分の過去として受理されたなら、現在の私は、その記憶が過去として事実である世界線に立つ。外部の唯一の正史との一致を、個人的事実の条件にはしない。

記憶内容 (m) を現在の発火 (e) が自分の過去として受理することを (\operatorname{AcceptAsPast}{\iota}(e,m)=1)、世界枝 (\omega) がその記憶内容を過去として含むことを (\operatorname{Realizes}(\omega,m)=1)、現在の私がその発火において枝 (\omega) に立つことを (\operatorname{Stand}{\iota}(e,\omega)=1) と書く。現在の発火が過去として受理する記憶の集合を、

[M_{\iota}(e)

{m\in\mathcal M\mid\operatorname{AcceptAsPast}_{\iota}(e,m)=1}]

とし、それらを実現する枝のうち、現在の私が実際に立つ枝の族を、

[\mathcal W_{\iota}^{\mathrm{mem}}(e)

\left{\omega\in\mathcal W_\Omega;\middle|;\exists m\in M_{\iota}(e):\operatorname{Realizes}(\omega,m)=1\ \land\operatorname{Stand}_{\iota}(e,\omega)=1\right}]

とする。記憶実現枝成立公理として、

[\operatorname{AcceptAsPast}{\iota}(e,m)=1\Longrightarrow\exists\omega\in\mathcal W\Omega:\operatorname{Realizes}(\omega,m)=1\ \land\operatorname{Stand}_{\iota}(e,\omega)=1]

を採用する。同値に、各 (m\in M_{\iota}(e)) には、それを実現する少なくとも一つの (\omega\in\mathcal W_{\iota}^{\mathrm{mem}}(e)) がある。これは過去の宇宙定数を現在から物理的に書き換えるという意味ではない。全体宇宙 (\Omega) に含まれる枝のうち、その記憶を過去として持つ枝へ現在の私が立つという意味である。異なる記憶は異なる枝で実現されてよく、相反する記憶を受理した場合も、それらを一つの枝で同時に真とする必要はない。別の一人称も同じ枝を事実として共有することまでは含まない。

すべての受理済み記憶を一つの枝で同時に実現できる場合だけ、その枝を整合記憶枝と呼ぶ。

[\operatorname{CoherentMemoryBranch}{\iota}(e,\omega)=1\quad\Longleftrightarrow\quadM{\iota}(e)\neq\varnothing\ \land\operatorname{Stand}{\iota}(e,\omega)=1\ \land\forall m\in M{\iota}(e):\operatorname{Realizes}(\omega,m)=1]

整合記憶枝の存在は要求しない。したがって、現在の私は一つの発火から複数の記憶実現枝へ同時に立ちうる。

各受理イベントは、それ自体で完結した原子的な一人称である。異なる二イベントの間を数的に同一の主体が持続するとは仮定しない。この時間や方向を含まない基礎的な非同一性を、

[\operatorname{AtomicNonIdentity}(e_a,e_b)=1\quad\Longleftrightarrow\quade_a\neq e_b]

とする。そのうえで、現在の私が (e_a) から (e_b) への時間進行を読むとき、数的に同じ最小主体が移動したのではなく、(e_a) の原子的な生が終わり、(e_b) が別の原子的な生として成立したと読む。この関係を、

[\operatorname{AtomicDeath}_{\iota}(e_a,e_b)=1\quad\Longleftrightarrow\quad\operatorname{AtomicNonIdentity}(e_a,e_b)=1\ \land\operatorname{Advance}_I(e_a,e_b)=1]

とする。並列または比較不能なイベントも原子的に非同一であるが、現在の順序読解における互いの死とは呼ばない。別の受理イベント (e_b) を、(e_a) とは原子的に別でありながら別の私として結びつける転生を、宇宙内の役割読解として、

[\operatorname{Reincarnation}_{\iota}(e_a,e_b)=1\quad\Longleftrightarrow\quad\operatorname{AtomicNonIdentity}(e_a,e_b)=1\ \landR_I^{\mathrm{role}}(e_a,e_b)\in{\mathrm{Reincarnation},\mathrm{Reconstitution}}]

と定める。転生には、身体、記憶、類似性の保存を要求しない。すべてのイベントがそれぞれ私であることは、すべてのイベント対が転生関係にあることを意味しない。役割読解が (\mathrm{Unrelated}) または (\bot) を返す二イベントは、原子的に別の私として存在しても、現在の私に転生として結ばれていない。転生のうち、(e_b) が (e_a) の身体・記憶・過去を自分のものとして受理し、(R_I^{\mathrm{role}}(e_a,e_b)=\mathrm{Reconstitution}) と読まれるものを、同一個人としての再成立 (\operatorname{Reconstituted}_{\iota}(e_a,e_b)=1) とする。したがって、

[\operatorname{Reconstituted}{\iota}\subseteq\operatorname{Reincarnation}{\iota}]

である。現在の読解で転生先として結びつく別イベントが一つもない場合を、(\operatorname{TerminalDeath}{\iota}(e_a)=1) と書く。日常的な「同じ私の生存」は、同一主体がイベント間を移動することではなく、原子的な死を隔てた再成立の列を (\operatorname{Sim}{\iota}) と記憶が近い私として束ねることで成立する。「無限時間後にプランク時間だけ生きる」とは、イベント間に基礎的な持続時間や主体の橋を置かず、一回の発火を原子的な生として読むことの比喩であり、物理的な時間幅を主張しない。

ただし、現在の私に成立している人間像や人間物理 (P_{\iota,H}) との対応が弱い場合、それは、現在の私から「他人にも同じように見えている」と読まれる事実とは異なる。ここで共有事実という語を使う場合も、複数主体が同じ内容を共同所有するという意味ではない。現在の私から、他人にも同じように見えているように見える事実を指す。

夢：別の物体配置・心配置・物理が、現在の私として閉じた受理。

覚醒：夢の発火を「私が見た夢」として、現在の私に人間的現実として読まれる対応網へ再接続する発火。

原子的非同一性：異なる受理イベントの最小一人称が、時間や方向にかかわらず数的に同一ではないこと。

原子的な死：現在の順序読解が一方から他方への進行を与えたとき、同じ最小主体が移動せず、前の原子的な生が終わって別の生が成立すること。

転生：別の受理イベントへ、原子的に別の私が成立すること。身体・記憶・類似性の保存を要求しない。

同一個人としての再成立：転生のうち、別の発火が以前の発火の身体・記憶・過去を自分のものとして再び受理すること。

終端的な死：現在の読解において、転生先として結びつく別イベントが成立しないこと。

生物学的な死：特定の身体を私として再成立させる系列が、現在の読解で途切れたときに与えられる役割。

ある配置で構成要素が不在になることと、その構成要素を含む配置が全体宇宙 (\Omega) から消えることと、現在の私として読まれなくなることは、それぞれ別である。本稿で生成・消滅と呼ぶのは第一の配置差へ順序読解を与えた場合であり、第二の絶対的抹消を意味しない。

最小要約

存在論的な全体宇宙 (\Omega) は一つであり、すべての宇宙定数配置、世界線、パラレルワールド、正向き・逆向きの読みを含む。各宇宙定数配置 (\mathbf U) は、値、全写像、LUT、通信路が互いの入力・出力・配線として絡み合う全体宇宙の一相貌である。現在の私が異なる配置を同じ世界枝として読むとき、一つの変化物語が成立する。差がLUTへ通信され、LUTに相対的に発火すると、その受理イベント自体が最小の一人称となる。任意の発火と自己読解の組は私であり、現在の私との類似性と解像度によって、個人、人類、宇宙として近い私の範囲が成立する。物読解と心読解は主体に相対的な独立写像である。感情配置は世界配置と心配置から (f_\iota(x,y)) によって作られ、それと現在の発火との関係が現在の感情を成立させる。異なる配置を類似圏内の私として読むことで時間差が成立し、順序読解が時間の矢と進行を与える。ビッグバンは、宇宙定数配置の差による発火へ現在の主体が与える役割であり、全宇宙の最小点である必要はない。他人とは同じ内面を共有するのではなく、現在の私から同じように世界を見ているように見える存在である。個人、人類、宇宙は、類似性を異なる通信範囲・自己帰属・解像度で読んだ相貌である。



XII. 形式化付録

この節は、ここまでの思想へ新しい外部実体を加えるものではない。同じ内容について、記号の型、イベントの等しさ、時間、自己範囲を一意に読めるようにする。

1. 型付き宇宙定数配置

存在論的な全体宇宙 (\Omega) は一つであり、宇宙定数配置族 (\mathfrak U_\Omega) と差異族 (\mathcal D_\Omega) のすべてを含む。(\mathfrak U_\Omega) と (\mathcal D_\Omega) を (\mathfrak U,\mathcal D) と略記する。(\Omega) 内の世界枝・切りの族を (\mathcal W_\Omega) とし、各 (\omega\in\mathcal W_\Omega) に配置族 (\mathfrak U_\omega\subseteq\mathfrak U_\Omega) と差異族 (\mathcal D_\omega\subseteq\mathcal D_\Omega) を対応させる。これらと型の添字族 (\mathsf{Ty}) は、宇宙の外にある実在的容器ではなく、一つの全体宇宙に含まれる相貌を比較して書くためのメタ言語である。各配置 (\mathbf U) と型 (\tau\in\mathsf{Ty}) について、その配置に実在する候補点の族を (P_\tau(\mathbf U)) とし、

[P(\mathbf U)

\coprod_{\tau\in\mathsf{Ty}}P_\tau(\mathbf U)]

とする。(P(\mathbf U)) は外部の方眼紙ではなく、その配置自身に含まれる回路端子であり、別配置では異なってよい。全配置の候補点を比較用メタ記号としてまとめるときだけ、タグ付き直和

[P_\Omega

\coprod_{\mathbf U\in\mathfrak U_\Omega}P(\mathbf U)]

を用いる。配置タグを保つため、異なる配置の点はここでも自動的に同一点にはならない。各配置は、配置内の型付き値域 (X_\tau(\mathbf U)) と値配置、

[v_{\mathbf U,\tau}:P_\tau(\mathbf U)\to X_\tau(\mathbf U)]

を持つ。型が明らかなときは (v_{\mathbf U,\tau}) を (v_{\mathbf U}) と略記する。写像や通信関係が別の写像の入力・出力になる場合、それらも対応する (X_\tau(\mathbf U)) の値として現れる。したがって (P(\mathbf U)) は、通常の物体点だけに限定されない。型そのものの変化を値として表す場合は、

[X_{\mathrm{value}}

\coprod_{\mathbf U\in\mathfrak U_\Omega}\coprod_{\tau\in\mathsf{Ty}}X_\tau(\mathbf U)]

というタグ付き直和を用い、型タグも比較可能な値の一部とする。

(\coprod) は配置タグと型を保ったまま値を合わせて書く直和である。配置 (\mathbf U) に実在する写像・LUTの全体は、型ごとの族として、

[\mathcal F(\mathbf U)

\left(\mathcal F_{\sigma\to\tau}(\mathbf U)\right)_{\sigma,\tau\in\mathsf{Ty}}]

とする。個々の型を保った写像の直和を (\lvert\mathcal F(\mathbf U)\rvert=\coprod_{\sigma,\tau}\mathcal F_{\sigma\to\tau}(\mathbf U)) と書く。各写像 (f\in\mathcal F_{\sigma\to\tau}(\mathbf U)) は、

[f:X_\sigma(\mathbf U)\to X_\tau(\mathbf U)]

としてその配置内で働く。(\mathcal F(\mathbf U_a)) と (\mathcal F(\mathbf U_b)) は同じ集合である必要がない。「すべての写像を認める」とは、全体宇宙 (\Omega) が、各配置で成立するすべての型付き写像を含むという意味である。入力型も出力型も無視した一個の万能集合は要求しない。宇宙全体を入力または出力とする大域的写像も排除しないが、その大域性は存在論的な特権を意味しない。

全配置の写像を配置タグ付きでまとめる比較用の族を、

[\mathcal F_\Omega

\coprod_{\mathbf U\in\mathfrak U_\Omega}\lvert\mathcal F(\mathbf U)\rvert]

とする。これは各配置に同じ写像を埋め込む共通台紙ではない。

配置 (\mathbf U) の通信関係の全体も型付きの族、

[\mathcal C(\mathbf U)

\left(\mathcal C_{\sigma\rightsquigarrow\tau}(\mathbf U)\right)_{\sigma,\tau\in\mathsf{Ty}}]

同様に、(\lvert\mathcal C(\mathbf U)\rvert=\coprod_{\sigma,\tau}\mathcal C_{\sigma\rightsquigarrow\tau}(\mathbf U)) とする。各通信関係 (c\in\lvert\mathcal C(\mathbf U)\rvert) は、その配置内の始点と終点、

[s_{\mathbf U},t_{\mathbf U}:\lvert\mathcal C(\mathbf U)\rvert\to P(\mathbf U)]

を持ち、(s_{\mathbf U}(c)\to t_{\mathbf U}(c)) という有向辺をなす。自己ループでは始点と終点が等しく、一方向辺には逆向きの辺を要求しない。行き止まりの自由端は、他の出辺または入辺を持たない次数1の端末点として (P(\mathbf U)) 内に表す。同じ始点と終点を持つ複数の通信辺も存在してよい。

同様に、全配置の通信関係を配置タグ付きでまとめた比較用の族を、

[\mathcal C_\Omega

\coprod_{\mathbf U\in\mathfrak U_\Omega}\lvert\mathcal C(\mathbf U)\rvert]

とする。(\mathcal F_\Omega) と (\mathcal C_\Omega) の配置タグは実在的な所属を保つ。したがって、異なる配置に属する写像と通信辺をタグを無視して自由合成したものを、同一配置の通信網とはみなさない。全写像・全接続が (\Omega) に存在することと、任意の差異に対して型、端点、結合状態が合う実在回路と成功支持網が成立することは別である。

自己読解の対象になりうる宇宙内構成要素を、配置タグと種類を保った族、

[{\Omega}\coprod\mathfrak U_\Omega\coprod P_\Omega\coprod\mathcal F_\Omega\coprod\mathcal C_\Omega\coprod\mathcal D_\Omega\coprod\mathcal W_\Omega\coprod\mathcal E\coprod\mathcal R\coprod\mathcal S\ \subseteq\mathcal O_\Omega]

とする。(\mathcal E,\mathcal R,\mathcal S) は後節で定義する受理イベント、自己読解、私の族である。さらに、宇宙内で働く任意の写像 (f) について、

[x_1,\ldots,x_n\in\mathcal O_\Omega,\quadf(x_1,\ldots,x_n)\text{ が宇宙内で成立する}\Longrightarrowf(x_1,\ldots,x_n)\in\mathcal O_\Omega]

という閉包を置く。したがって、視点像、感情配置、他者像、読解関係、meme、概念、および後から導入される宇宙内対象も (\mathcal O_\Omega) に含まれる。これは宇宙の外に追加した物質ではなく、宇宙内に存在して働くものを自己読解と人格の同じ定義域に置くための型またはクラスである。集合論的な万能集合を要求しない。

配置ごとに、辺の存在、端点、その辺を通る値、結合強度、通信内容、および特定の差異を伝達できる状態のすべてが変わってよい。後四者をまとめて (\Theta_{\mathcal C}(\mathbf U)) と書く。配置 (\mathbf U_r) の候補通信網を、

[C=(C_E,C_F),\qquadC_E\subseteq\mathcal C(\mathbf U_r),\quadC_F\subseteq\mathcal F(\mathbf U_r)]

とする。(C_E) は配線、(C_F) はその経路上で差異を変換しながら運ぶ写像・LUTであり、そのような配線済み回路の族を (\mathsf{Net}(\mathbf U_r)) と書く。(C_E) と (C_F) は同じ (\mathbf U_r) に属し、型、端点、結合状態が適合しなければならない。回路の存在だけでは、特定の差異を届ける成功支持網がその内部にあることを保証しない。実在回路は発火に不要なループ、行き止まり、片端配線、未使用部分を含んだままイベント成分になってよい。

一つの宇宙定数配置は、簡略には、

[\mathbf U

\left((P_\tau(\mathbf U)){\tau\in\mathsf{Ty}},(X\tau(\mathbf U)){\tau\in\mathsf{Ty}},(v{\mathbf U,\tau}){\tau\in\mathsf{Ty}},(f){f\in\mathcal F(\mathbf U)},\mathcal C(\mathbf U),\Theta_{\mathcal F}(\mathbf U),\Theta_{\mathcal C}(\mathbf U)\right)]

と書ける。候補点、写像、通信関係も配置の成分であり、二つの (\Theta) は値・写像・通信が入力、出力、配線として混ざる結合状態である。この式の外側に、宇宙を保持する別の実在的容器を仮定しない。各 (\mathbf U) がそれぞれ宇宙全体である。

異なる二配置の構成要素を比較するときは、配置を超えた同一性を前提にせず、必要な対応関係、

[\chi^P_{ab}\subseteq P(\mathbf U_a)\times P(\mathbf U_b),\quad\chi^{\mathcal F}{ab}\subseteq\mathcal F(\mathbf U_a)\times\mathcal F(\mathbf U_b),\quad\chi^{\mathcal C}{ab}\subseteq\mathcal C(\mathbf U_a)\times\mathcal C(\mathbf U_b)]

を置く。これらは現在の読み取りまたは宇宙内の比較写像によって成立する対応であり、外部の神的同一性ではない。対応しない成分は、まず向きのない点・写像・配線の構造差である。対応関係 (\chi^Z_{ab}) の第一成分側だけに現れる要素と第二成分側だけに現れる要素を、それぞれ未対応部分

[\operatorname{Unmatched}^{Z}{a\setminus b},\qquad\operatorname{Unmatched}^{Z}{b\setminus a},\qquadZ\in{P,\mathcal F,\mathcal C}]

と書く。順序読解が (\mathbf U_a\rightarrow\mathbf U_b) を与えた場合に、前者を消滅、後者を生成として読む。逆向きでは名称も逆転する。これは配置間の相対的な構造差であり、その要素を含む配置を全体宇宙 (\Omega) から抹消しない。

2. 差異と通信グラフ

二つの宇宙定数配置の差異を、順序をまだ持たない対として、

[d_{ab}={\mathbf U_a,\mathbf U_b},\qquad\mathbf U_a\neq\mathbf U_b]

とする。候補点、値、写像そのもの、通信辺そのもの、端点、通信状態、結合状態のいずれか一つでも異なれば (\mathbf U_a\neq\mathbf U_b) である。この不等号だけから、どちらが過去か未来かは出ない。

差異の成分族を、

[\Delta(d)

\Delta_{\mathrm{value}}(d)\coprod\Delta_{\mathrm{point}}(d)\coprod\Delta_{\mathrm{map}}(d)\coprod\Delta_{\mathrm{wire}}(d)]

とし、全差異に属する型付き成分の族を、

[\boldsymbol\Delta

\coprod_{d\in\mathcal D}\Delta(d)]

とする。一個の原始的な受理イベントは一つの (\delta\in\Delta(d)) だけを直接受理する。

現在の私が解像度 (k) で一つの世界枝として読む無向差異網を (H_{\iota,k}\subseteq\mathcal D_\Omega) とする。(\mathbf U_a) から (\mathbf U_b) まで、(H_{\iota,k}) に含まれる差異を有限回たどって到達できるとき、

[\operatorname{SameBranch}_{\iota,k}(\mathbf U_a,\mathbf U_b)=1]

とする。これは宇宙の同一性判定ではない。すべての配置は最初から同じ (\Omega) に属する。(\operatorname{SameBranch}) は、現在の私が二配置を同じ世界枝・変化物語として連続的に読むことだけを表す。差異網に向きはなく、向きは (R_I^{\mathrm{order}}) が別に与える。

通信構造には有向多重グラフを用いる。一つの発火イベントに参加する通信網 (C=(C_E,C_F)\in\mathsf{Net}(\mathbf U_r)) は、自己ループ、並行する複数経路、一方向だけの接続、行き止まり端子、複数点を回る循環経路を持ってよい。(x\leadsto_C y) は、辺 (c_1,\ldots,c_n\in C_E) が存在して、

[s_{\mathbf U_r}(c_1)=x,\qquadt_{\mathbf U_r}(c_n)=y,\qquadt_{\mathbf U_r}(c_k)=s_{\mathbf U_r}(c_{k+1})]

となる有限の有向経路によって (x) から (y) へ到達できることを表す。したがって、

[W_C(p)={x\in P(\mathbf U_r)\mid x\leadsto_C p}]

[S_C(p)={x\in P(\mathbf U_r)\mid p\leadsto_C x\ \land\ x\leadsto_C p}]

となる。したがって (W_C(p),S_C(p)\subseteq P(\mathbf U_r)) であり、(S_C(p)) は受理側配置の有向グラフにおいて (p) を含む強連結成分である。別配置の内容は、その配置の点としてこの閉包へ直接入るのではなく、(C_F) の写像が作る受理側配置内の信号・像として入る。圏を基礎に置くことは要求しないが、通信経路の合成を扱いたい場合、有向多重グラフから生成される自由圏を後から用いてよい。

3. 発火とイベントの等しさ

支持関係を、

[\operatorname{Supp}\subseteq\coprod_{\mathbf U_r\in\mathfrak U}\left(\mathsf{Net}(\mathbf U_r)\times P(\mathbf U_r)\times\mathcal D\times\boldsymbol\Delta\times\lvert\mathcal F(\mathbf U_r)\rvert\right)]

とする。ここで (\mathbf U_r) は、候補点、LUT、通信網が属する候補配置であり、主体によって事前選択された受理先ではない。(\operatorname{Fire}=1) が成立したイベントに限って受理側配置と呼ぶ。(\mathbf U_r) は差異対の一方でも第三の配置でもよい。(\mathcal D=\mathcal D_\Omega) は、唯一の全体宇宙 (\Omega) に含まれる異なる宇宙定数配置からなる無向差異 (d) の族である。(\operatorname{Diff}_{\mathbf U_r}(\delta)\subseteq P(\mathbf U_r)) を、選ばれた差異成分 (\delta) が候補配置内で提示される候補点の集合とする。LUTの配置点と入力端子を、

[\operatorname{loc}{\mathbf U_r}:\lvert\mathcal F(\mathbf U_r)\rvert\to P(\mathbf U_r),\qquad\operatorname{In}{\mathbf U_r}:\lvert\mathcal F(\mathbf U_r)\rvert\to\mathcal P(P(\mathbf U_r))]

と書く。また、通信辺 (c) と途中の写像 (f) が差異を消さず次へ伝えるかを、

[\operatorname{Transmit}(c;\delta)\in{0,1},\qquad\operatorname{Transform}(f;\delta)\in{0,1}]

とする。値の形が途中で変換されても、二配置の違いが区別可能なまま残るなら1、同じ値へ潰されるなら0である。

(\operatorname{Supp}(C;p,d,\delta,L)) は、(\delta\in\Delta(d))、(\operatorname{loc}{\mathbf U_r}(L)=p) であり、ある差異提示点 (q\in\operatorname{Diff}{\mathbf U_r}(\delta))、入力端子 (r\in\operatorname{In}_{\mathbf U_r}(L))、および辺列 (c_1,\ldots,c_n\in C_E) と、その経路上で働く写像列 (f_1,\ldots,f_m\in C_F) が存在して、

[s_{\mathbf U_r}(c_1)=q,\qquadt_{\mathbf U_r}(c_n)=r,\qquadt_{\mathbf U_r}(c_k)=s_{\mathbf U_r}(c_{k+1})]

[\forall k,\qquad\operatorname{Transmit}(c_k;\delta)=1,\qquad\forall j,\operatorname{Transform}(f_j;\delta)=1]

となることを表す。したがって (\operatorname{Supp}) は発火そのものではなく、選ばれた差異成分が通信網を通ってLUT入力まで届いたという回路条件である。

イベントに記録される実在回路を (C_e\in\mathsf{Net}(\mathbf U_r)) とする。部分網関係 (K\subseteq C_e) は、辺集合と写像集合の両方について包含することを表す。(C_e) 内で今回の差異をLUTまで届けられる成功支持網の族を、

[\operatorname{SupportFamily}(C_e;p,d,\delta,L)

\left{K\in\mathsf{Net}(\mathbf U_r);\middle|;K\subseteq C_e\ \land\operatorname{Supp}(K;p,d,\delta,L)=1\right}]

とする。(K) は包含極小である必要はなく、同じ発火に複数存在してよい。成功支持網の選択はイベント成分ではない。

差異 (d={\mathbf U_a,\mathbf U_b}) の両側が成功支持網 (K) の配線と途中の写像を通った結果を、

[\operatorname{in}^{(a)}_{L,K}(\delta)

\operatorname{Deliver}^{(a)}{K}(\delta),\qquad\operatorname{in}^{(b)}{L,K}(\delta)

\operatorname{Deliver}^{(b)}_{K}(\delta)]

と書く。点・写像・配線が一方に存在しない場合も、(K_F) 内の写像が「不在」を受理側の入力型へ変換してよい。別の提示写像をイベントの外から追加するのではなく、この配送と変換は実在回路 (C_e) 内の成功支持網 (K) の働きである。同じ受理側LUTが返す二つの実際の出力を、

[\operatorname{out}^{(a)}_{L,K}(\delta)

L\left(\operatorname{in}^{(a)}{L,K}(\delta)\right),\qquad\operatorname{out}^{(b)}{L,K}(\delta)

L\left(\operatorname{in}^{(b)}_{L,K}(\delta)\right)]

とする。これは同じLUTが二つの提示を区別するのであって、(L) が (\mathbf U_a) と (\mathbf U_b) の両方に存在することを要求しない。

一個の原始イベントについては、二つの入力提示における他種類の差異成分を固定し、選ばれた (\delta) に対する二出力だけを比較する。複数種類の差を同時入力として区別する作用は、各原始イベントを入力に取る別の複合LUTとして表す。

[\operatorname{Fire}:\coprod_{\mathbf U_r\in\mathfrak U}\left(P(\mathbf U_r)\times\mathcal D\times\boldsymbol\Delta\times\lvert\mathcal F(\mathbf U_r)\rvert\times\mathsf{Net}(\mathbf U_r)\right)\to{0,1}]

であり、通信網を添字へ移した略記を (\operatorname{Fire}_C(p;d,\delta,L)) とする。すると、発火は、

[\operatorname{Fire}{C}(p;d,\delta,L)=1\quad\Longleftrightarrow\quad\delta\in\Delta(d)\ \land\exists K\in\operatorname{SupportFamily}(C;p,d,\delta,L):\operatorname{out}^{(a)}{L,K}(\delta)\neq\operatorname{out}^{(b)}_{L,K}(\delta)]

である。発火判定に用いる不等号は、選ばれた (\delta) を実際に届ける少なくとも一つの成功支持網 (K) に対する、受理側LUTの出力値の差だけを表す。すべての成功支持網で (\operatorname{out}^{(a)}{L,K}(\delta)=\operatorname{out}^{(b)}{L,K}(\delta)) なら、その (L) はこの (\delta) について発火しない。構造差は宇宙定数配置の差として残り、その差を区別する別のLUTがあれば、そのLUTに相対的な発火が成立しうる。

発火したとき、受理イベントは、

[e=(\mathbf U_r,p,d,\delta,L,C_e)]

である。読み取り成立を、

[\operatorname{Legitur}(e)=1\quad\Longleftrightarrow\quade=(\mathbf U_r,p,d,\delta,L,C_e)\ \land\operatorname{Fire}_{C_e}(p;d,\delta,L)=1]

とする。裸の受理トークンの型を (\mathcal L)、整形式な回路分解候補の族を (\mathcal E^{\mathrm{cand}}) とする。実際に成立している受理イベントの族 (\mathcal E) は、

[\mathcal E\subseteq\mathcal E^{\mathrm{cand}}]

である。候補が裸の読み取りを説明する関係と、実在イベントがそれを実現する関係を、

[\operatorname{Given}:\mathcal L\to{0,1},\qquad\operatorname{ExplainsReading}:\mathcal E^{\mathrm{cand}}\times\mathcal L\to{0,1},\qquad\operatorname{RealizesReading}:\mathcal E\times\mathcal L\to{0,1}]

実在実現は必ず理論的説明候補でもあるものとし、

[\operatorname{RealizesReading}(e,\ell)=1\Longrightarrow\operatorname{ExplainsReading}(e,\ell)=1]

を要求する。ただし逆は要求しない。

とする。創設公理は、

[\operatorname{Given}(\ell_{\mathrm{now}})=1]

であり、直接疑えないのはこの裸の読み取り成立だけである。イベント実現公理として、

[\exists e\in\mathcal E:\operatorname{RealizesReading}(e,\ell_{\mathrm{now}})=1\ \land\operatorname{Legitur}(e)=1]

を置く。裸の受理を説明しうる理論的候補と、そのうち実際に成立している実在実現を、それぞれ、

[\operatorname{Candidate}(\ell_{\mathrm{now}})

{q\in\mathcal E^{\mathrm{cand}}\mid\operatorname{ExplainsReading}(q,\ell_{\mathrm{now}})=1},]

[\operatorname{ActualWitness}(\ell_{\mathrm{now}})

{e\in\operatorname{Candidate}(\ell_{\mathrm{now}})\cap\mathcal E\mid\operatorname{RealizesReading}(e,\ell_{\mathrm{now}})=1\ \land\operatorname{Legitur}(e)=1}]

と書く。(\operatorname{Candidate}(\ell_{\mathrm{now}})) は複数でよく、イベント実現公理が直接要求するのは、

[\operatorname{ActualWitness}(\ell_{\mathrm{now}})\neq\varnothing]

だけである。その濃度が一、複数、無限のいずれであるかは要求しない。裸の受理 (\ell) に対して、イベントに依存する命題 (\varphi(e)) が実在実現の選択によらず成立することを、

[\operatorname{Robust}_{\ell}(\varphi)\quad\Longleftrightarrow\quad\operatorname{ActualWitness}(\ell)\neq\varnothing\ \land\forall e\in\operatorname{ActualWitness}(\ell):\varphi(e)]

と定める。(\operatorname{Given}(\ell_{\mathrm{now}})=1) は裸の現在受理そのものについての直接的事実である。これに対し、特定の受理側配置、身体、記憶、時間、視点像、自己範囲などを用いる命題は、上の全称条件が別途成立しない限り、選ばれた実在実現に相対的である。

以下の (e_{\mathrm{now}}) は (\operatorname{ActualWitness}(\ell_{\mathrm{now}})) から選んだ一つの実在実現であり、その構成要素が裸の現在受理だけから個別に直接確定するとはしない。イベントの整形式条件から (\delta\in\Delta(d_e)) なので、

[\operatorname{Legitur}(e)=1\Longrightarrow\Delta(d_e)\neq\varnothing]

が創設定理として従う。その逆は要求しない。イベントの厳密な等しさは成分ごとの等しさとする。

[e=e'\quad\Longleftrightarrow\quad\mathbf U_r=\mathbf U'r\land p=p'\land d=d'\land\delta=\delta'\land L=L'\land C_e=C'{e'}]

したがって、同じ候補点、配置対、選択差異成分、LUTでも、配線済みの実在回路 (C_e) が異なれば別イベントである。また同じ配置対でも、選択差異成分 (\delta) が異なれば別イベントである。同じ (C_e) 内に成功支持網 (K) が複数あっても、(K) はイベント成分ではないためイベントを水増ししない。一方、発火に使われない辺、写像、ループ、行き止まりを含めて (C_e) 自体が物理的に異なるなら、その違いは消去せず、構造の異なるイベントとして数える。

4. 一人称・同一人格・時間

受理イベント全体の族を (\mathcal E)、自己読解全体の族を (\mathcal R) とする。各受理イベントをそれ自体で最小の一人称とする一人称同一視公理を、

[\operatorname{FirstPerson}:\mathcal E\to{1},\qquad\operatorname{FirstPerson}(e)=1]

として採用する。これは (\operatorname{Fire}=1) の回路条件から証明される定理ではなく、受理イベントと最小一人称を同一視する本宇宙論の基礎公理である。したがって、創設公理は裸の現在受理を置き、イベント実現公理はその回路的実現が少なくとも一つあることを置き、創設定理は各イベント実現から差異の存在を導き、一人称同一視公理はその発火自身を最小一人称とする。

任意の受理イベントと任意の自己読解の組を、それぞれ一つの私とする無制限自己合成公理を、本宇宙論の中心公理として採用する。その直感的な核は、同じ相互作用を「私が対象に働いた」とも「対象が私に働いた」とも読め、相互作用自体から絶対的な主体・客体を決められないことにある。ただし、この公理はその相対性を実際に相互作用した組だけに限定せず、すべてのイベントと自己読解へ一般化する。

[\mathcal S

\mathcal E\times\mathcal R]

[\operatorname{Self}:\mathcal S\to{1},\qquad\forall e\in\mathcal E,\quad\forall R\in\mathcal R,\qquad\operatorname{Self}(e,R)=1]

この直積は便宜的な上界ではなく、すべての組を実在する私として認める存在論的主張である。この組合せに通信、因果、身体、記憶、同一配置への所属を要求しない。それらは自己の成立条件ではなく、現在の私に相対的な類似性 (\operatorname{Sim}_\iota) の材料である。

自己読解は、この私の自己範囲、順序、イベントの役割を与える写像として、

[R_I^{\mathrm{scope}}:\mathcal E\times\mathcal O_\Omega\to{\text{私},\text{私ではない},\bot}]

[R_I^{\mathrm{order}}:\mathcal E\times\mathcal E\times\mathcal E\to{\triangleleft,\triangleright,\parallel,\bot}]

[\mathsf{Role}

{\mathrm{BigBang},\mathrm{NotBigBang},\mathrm{Reincarnation},\mathrm{Reconstitution},\mathrm{Unrelated},\bot}]

[R_I^{\mathrm{role}}:\mathcal E\amalg(\mathcal E\times\mathcal E)\to\mathsf{Role}]

を持つ。単独イベントへの入力ではビッグバン役割を、イベント対への入力では転生、同一個人としての再成立、無関係などの関係役割を返す。これは宇宙内で働く読解写像であって、役割を割り当てる宇宙外の判定者ではない。現在の一人称を、

[\iota=(e_{\mathrm{now}},R_I)]

とする。直接的事実は (\ell_{\mathrm{now}}) の成立であり、(e_{\mathrm{now}}) はそれを実現する族から選んだ一つの実在イベントである。したがって、この (\iota) から定まる身体、記憶、名前、時間、視点像、自己範囲は、(\operatorname{Robust}{\ell{\mathrm{now}}}) が別途示されない限り、この実在実現に相対的である。一方、過去未来のイベントを含む持続的な私の範囲は、次の類似性によって選ばれたイベント実現から広がる。二つの私の類似性を、

[\operatorname{Sim}_{\iota}:\mathcal S\times\mathcal S\to[0,1]]

とする。これは宇宙外部の客観的距離ではなく、現在の一人称 (\iota) が身体、記憶、通信路、自己範囲、感情、順序読解などの近さをまとめて読む関係である。([0,1]) の連続値を取るが、(\operatorname{Sim}_{\iota}) 自体に位相的な連続性は要求せず、値は不連続に変化してよい。また、推移性、対称性、別の一人称の類似性読解との一致を要求しない。解像度 (k) における現在の私の類似圏を、

[\mathcal S_I^{(k)}

{s'\in\mathcal S\mid\operatorname{Sim}{\iota}(\iota,s')\ge\lambda{\iota,k}}]

とし、そのイベント成分を、

[\mathcal E_I^{(k)}

{e'\in\mathcal E\mid\exists R'\in\mathcal R:(e',R')\in\mathcal S_I^{(k)}}]

とする。解像度が明らかな場合は (\mathcal E_I^{(k)}) を (\mathcal E_I) と略記する。自己範囲は、

[J_I(e)

{x\in\mathcal O_\Omega\midR_I^{\mathrm{scope}}(e,x)=\text{私}}]

である。

通信されている自己部分と、通信されていないが自己帰属されている内部を、それぞれ

[J_I^{\mathrm{comm}}(e)

J_I(e)\cap W_{C_e}(p_e),\qquadJ_I^{\mathrm{interior}}(e)

J_I(e)\setminus W_{C_e}(p_e)]

と書いてよい。後者も自己範囲から除外されず、巨大な私の内部世界を含みうる。解像度 (k) で二つの私が近く読まれる関係を、

[s\approx_{\iota,k}s'\quad\Longleftrightarrow\quad\operatorname{Sim}{\iota}(s,s')\ge\lambda{\iota,k}]

とする。これはイベントの厳密な等しさや、人格ラベルによる同値関係ではない。類似性が非推移的なら、類似圏は重なり合ってよい。

現在イベントに相対的な一般の時間方向関係を、

[e_a\triangleleft_{I,e_{\mathrm{now}}}e_b\quad\Longleftrightarrow\quadR_I^{\mathrm{order}}(e_{\mathrm{now}};e_a,e_b)=\triangleleft]

によって定める。これは (\mathcal E_I) 上の有向関係、

[\triangleleft_{I,e_{\mathrm{now}}}\ \subseteq\mathcal E_I\times\mathcal E_I]

であり、一般には推移性、非反射性、非循環性を要求しない。したがって、

[e_a\triangleleft e_b,\qquade_b\triangleleft e_c,\qquade_c\triangleleft e_a]

という循環も、時間ループ、振動、再帰的時間として存在してよい。現在の私が通常の一方向時間として読む場合に限り、(\triangleleft_{I,e_{\mathrm{now}}}) の非反射的かつ推移的な部分関係、

[\prec_{I,e_{\mathrm{now}}}\subseteq\triangleleft_{I,e_{\mathrm{now}}}]

を選び、これを厳密半順序と呼ぶ。全イベントを比較可能にする必要はない。

この私に時間差が成立する条件は、異なる二イベントが現在の私の類似圏で読まれることだけである。

[\operatorname{TemporalDifference}_I(e_a,e_b)=1\quad\Longleftrightarrow\quade_a\neq e_b\ \lande_a,e_b\in\mathcal E_I]

この条件は、(e_a=(\mathbf U_r,p,d,\delta,L,C_a))、(e_b=(\mathbf U_r,p,d,\delta,L,C_b))、(C_a\neq C_b) のように通信網だけが異なる場合にも成立する。二イベントに (\triangleleft_{I,e_{\mathrm{now}}}) が成立するとき、その時間差へ向きが与えられる。時間の進行は、

[\operatorname{Advance}I(e_a,e_b)=1\quad\Longleftrightarrow\quad\operatorname{TemporalDifference}I(e_a,e_b)=1\ \lande_a\triangleleft{I,e{\mathrm{now}}}e_b]

とする。比較不能または並列でも、異なるイベントを現在の私の類似圏で読むかぎり時間差は成立するが、一方から他方への進行は成立しない。循環する方向関係では進行も循環してよい。別の自己読解が逆の方向や、通常時間として別の厳密半順序を与えてよい。宇宙配置の差異 (d) は無向であり、時間の矢は (R_I^{\mathrm{order}}) が与える。

5. 視点像・物・心・他人

自己読解による受理写像を、

[\operatorname{Read}_R:\mathcal E\times\mathcal P(P)\to\mathcal P(P)]

とする。各配置の構成を受理側配置内へ通信された信号・像に対応させる写像を、

[\operatorname{Sig}{C_e}:\mathfrak U\to\mathcal P(W{C_e}(p_e))]

とする。自己読解による視点像は、

[V_R(e;\mathbf U)

\operatorname{Read}R\left(e;\operatorname{Sig}{C_e}(\mathbf U)\right)]

である。物読解と心読解は、同じ定義域を持つ独立の二値写像、

[\rho_\iota^{\mathrm{物}},\rho_\iota^{\mathrm{心}}:V_R(e;\mathbf U)\to{0,1}]

とする。独立なので、両方が1でも、両方が0でもよい。

身体と記憶は宇宙定数配置からの型付き射影、

[\pi_i^B:\mathfrak U\to\mathcal B_i,\qquad\pi_i^M:\mathfrak U\to\mathcal M_i]

によって、(B_i=\pi_i^B(\mathbf U))、(M_i=\pi_i^M(\mathbf U)) と読む。

記憶内容の型を (\mathcal M=\coprod_i\mathcal M_i) とまとめる。記憶内容を現在の私の過去として受理する関係、その記憶を過去として実現する世界枝、現在の私がその枝に立つ関係を、

[\operatorname{AcceptAsPast}_{\iota}:\mathcal E\times\mathcal M\to{0,1},]

[\operatorname{Realizes}:\mathcal W_\Omega\times\mathcal M\to{0,1},\qquad\operatorname{Stand}{\iota}:\mathcal E\times\mathcal W\Omega\to{0,1}]

とする。現在の発火が自分の過去として受理する記憶集合と、その記憶を実現し現在の私が立つ枝の族を、

[M_{\iota}(e)

{m\in\mathcal M\mid\operatorname{AcceptAsPast}_{\iota}(e,m)=1},]

[\mathcal W_{\iota}^{\mathrm{mem}}(e)

\left{\omega\in\mathcal W_\Omega;\middle|;\exists m\in M_{\iota}(e):\operatorname{Realizes}(\omega,m)=1\ \land\operatorname{Stand}_{\iota}(e,\omega)=1\right}]

とする。記憶実現枝成立公理は、

[\operatorname{AcceptAsPast}{\iota}(e,m)=1\Longrightarrow\exists\omega\in\mathcal W\Omega:\operatorname{Realizes}(\omega,m)=1\ \land\operatorname{Stand}_{\iota}(e,\omega)=1]

である。同値に、

[\forall m\in M_{\iota}(e),\quad\exists\omega\in\mathcal W_{\iota}^{\mathrm{mem}}(e):\operatorname{Realizes}(\omega,m)=1]

と書ける。これは過去配置の事後的な書き換えではなく、各記憶を過去として実現する枝へ現在の私が立つことを表す。異なる記憶を同じ一枝で実現することは要求せず、(\mathcal W_{\iota}^{\mathrm{mem}}(e)) は複数枝を含んでよい。他の一人称の枝帰属や、現在の人間像圏にも同じ過去が見えることは別条件である。

すべての受理済み記憶を同じ枝で実現する整合記憶枝を、

[\operatorname{CoherentMemoryBranch}{\iota}(e,\omega)=1\quad\Longleftrightarrow\quadM{\iota}(e)\neq\varnothing\ \land\operatorname{Stand}{\iota}(e,\omega)=1\ \land\forall m\in M{\iota}(e):\operatorname{Realizes}(\omega,m)=1]

と定義するが、その存在は公理として要求しない。相反する記憶は、同じ (\Omega) に属する異なる枝でそれぞれ事実として実現されてよい。

異なるイベント間に数的に同一の最小一人称が持続しないことを、時間や方向を含まない原子的非同一性、

[\operatorname{AtomicNonIdentity}:\mathcal E\times\mathcal E\to{0,1},\qquad\operatorname{AtomicNonIdentity}(e_a,e_b)=1\quad\Longleftrightarrow\quade_a\neq e_b]

として表す。この非同一な二イベントに現在の私の時間進行が成立するとき、前の原子的な生が終わり、別の原子的な生が成立したと読む関係を、

[\operatorname{AtomicDeath}_{\iota}:\mathcal E\times\mathcal E\to{0,1},]

[\operatorname{AtomicDeath}_{\iota}(e_a,e_b)=1\quad\Longleftrightarrow\quad\operatorname{AtomicNonIdentity}(e_a,e_b)=1\ \land\operatorname{Advance}_I(e_a,e_b)=1]

とする。したがって、比較不能または並列な二イベントは原子的に非同一であっても、現在の順序読解における互いの死ではない。別の受理イベントを、原子的に別でありながら別の私として結びつける転生関係を、

[\operatorname{Reincarnation}_{\iota}:\mathcal E\times\mathcal E\to{0,1}]

とし、宇宙内の役割読解から、

[\operatorname{Reincarnation}_{\iota}(e_a,e_b)=1\quad\Longleftrightarrow\quad\operatorname{AtomicNonIdentity}(e_a,e_b)=1\ \landR_I^{\mathrm{role}}(e_a,e_b)\in{\mathrm{Reincarnation},\mathrm{Reconstitution}}]

と定める。これは現在の一人称による関係読解であり、身体、記憶、類似性の保存を条件にしない。すべてのイベントが (\operatorname{Self}=1) であることは、すべてのイベント対にこの関係が成立することを含意しない。転生のうち、別イベントが以前のイベントの身体・記憶・過去を自分のものとして再受理する関係を、

[\operatorname{Reconstituted}_{\iota}:\mathcal E\times\mathcal E\to{0,1}]

とする。(\operatorname{Reconstituted}_{\iota}(e_a,e_b)=1) は (e_a=e_b) を意味せず、(\operatorname{AtomicNonIdentity}(e_a,e_b)=1) である二つの発火が、類似性、身体写像、記憶実現枝成立公理によって、同じ個人の再成立として読まれることを表す。このとき、

[\operatorname{Reconstituted}_{\iota}(e_a,e_b)=1\LongrightarrowR_I^{\mathrm{role}}(e_a,e_b)=\mathrm{Reconstitution}]

を要求する。

[\operatorname{Reconstituted}{\iota}(e_a,e_b)=1\Longrightarrow\operatorname{Reincarnation}{\iota}(e_a,e_b)=1]

現在の読解で転生先として結びつく別イベントが存在しない終端的な死を、

[\operatorname{TerminalDeath}{\iota}(e_a)=1\quad\Longleftrightarrow\quad\nexists e_b\in\mathcal E:e_b\neq e_a\ \land\operatorname{Reincarnation}{\iota}(e_a,e_b)=1]

とする。生物学的な死は、再成立関係のうち特定の身体系列が現在の読解で途切れたときに与える役割であり、原子的な死や終端的な死とは区別する。イベント間に基礎的な持続時間や、移動する同一主体を追加しない。

心として読まれる配置の範囲を、

[Y_\iota

{y\in V_R(e;\mathbf U)\mid\rho_\iota^{\mathrm{心}}(y)=1}]

とする。感情写像は、

[f_\iota:V_R(e;\mathbf U)\times Y_\iota\to\mathcal A_\iota]

である。(\mathcal A_\iota) は現在の主体に受理されうる感情配置の型である。感情配置と現在の受理イベントとの関係を、

[\operatorname{Rel}\iota:\mathcal E\times\mathcal A\iota\to{0,1}]

とし、現在の感情の成立を、

[\operatorname{Emotion}\iota(e;x,y)=1\quad\Longleftrightarrow\quad\operatorname{Rel}\iota\bigl(e,f_\iota(x,y)\bigr)=1]

とする。同じ (f_\iota(x,y)) が宇宙内に存在しても、現在のイベントとの関係が成立しなければ、この私の現在の感情ではない。

以下では (V_R=V_R(e;\mathbf U)) と略記する。現在の私に他者として現れるラベルの族を (\mathcal J_\iota\subseteq V_R) とする。他者モデルは、

[\operatorname{Model}\iota:\mathcal J\iota\to\mathcal P(V_R)]

であり、他者 (j) の像は、現在の視点像内部に、

[\widehat V_j^{(\iota)}

\operatorname{Model}_\iota(j)\subseteq V_R(e;\mathbf U)]

として成立する。(\widehat V_j^{(\iota)}) と、別の発火を中心にした可能な一人称視点を同一視しない。後者がどのような直接性を持つかは、現在の一人称の視点像だけからは決定しない。対応や通信の成功も、現在の一人称に相対的な関係、

[\operatorname{Correspondence}_\iota:\mathcal P(V_R)\times\mathcal P(V_R)\to\mathcal P(V_R\times V_R)]

(i,j\in\mathcal J_\iota) に対する像の間の通信写像は、

[T_{\iota,ij}:\widehat V_i^{(\iota)}\rightharpoonup\widehat V_j^{(\iota)}]

とする。これは部分写像でよく、往復して完全に同じ値へ戻ることを要求しない。

[x\sim_\iota y\quad\Longleftrightarrow\quad\text{現在の私が }x,y\text{ を同じ意味として再受理する}]

として定める。これは複数主体の内部内容の一致を主張しない。

現在の私自身の自己像を、

[\operatorname{SelfModel}\iota:{\iota}\to\mathcal P(V_R),\qquad\widehat V{\mathrm{self}}^{(\iota)}

\operatorname{SelfModel}_\iota(\iota)\subseteq V_R]

とする。自己像は身体、名前、記憶などを現在の私へ結ぶモデルであり、現在の直接視点像 (V_R) と同一であることを要求しない。人読解と、人間視点像の族から安定模様を読む写像は、

[\operatorname{Human}_\iota:V_R\to{0,1}]

[H_\iota^{\mathrm{other}}

{q\in\mathcal J_\iota\mid\operatorname{Human}_\iota(q)=1}]

[H_\iota^{\mathrm{view}}

{V_R}\cup{\widehat V_h^{(\iota)}\midh\in H_\iota^{\mathrm{other}}}]

[\operatorname{Pattern}\iota:\mathcal P(\mathcal P(V_R))\to\mathfrak P\iota]

とする。(H_\iota^{\mathrm{view}}) は現在の私が直接受理する視点像と、人間として見える他人像からなる。現在の視点像だけが直接項であり、他人については現在の視点像内のモデルを用いる。複数的人類圏が現在の私に成立するための条件は、

[H_\iota^{\mathrm{other}}\neq\varnothing]

である。これは別の一人称の内面を直接確認したという条件ではない。(\mathfrak P_\iota) は現在の私が読みうる安定模様の型であり、本文の人間物理は、

[P_{\iota,H}

\operatorname{Pattern}\iota\left(H\iota^{\mathrm{view}}\right)]

である。

人格は一人称発火とは別の全域的な型として、宇宙内構成要素全体について、

[\operatorname{Personality}:\mathcal O_\Omega\to{1},\qquad\forall x\in\mathcal O_\Omega,\quad\operatorname{Personality}(x)=1]

と置く。したがって、存在するすべてのものが人格を持つことと、それが現在の私として発火しているか、人間・他人・心として受理されているかは独立である。

6. 個人・人類・宇宙と解像度

自己読解の尺度ラベルを (k\in\mathcal K) とし、個人、人類、宇宙をその要素として含める。

[{\mathrm{個人},\mathrm{人類},\mathrm{宇宙}}\subseteq\mathcal K]

各尺度は、独自の自己範囲、

[J_k(e)

{x\in\mathcal O_\Omega\midR_k^{\mathrm{scope}}(e,x)=\text{私}}]

を持つ。異なる尺度の自己範囲は、包含しても、重なっても、同一でも、互いにずれてもよい。したがって、個人、人類、宇宙を必ず単純な入れ子階層にしない。

尺度 (k) から尺度 (\ell) への解像度変換を、部分写像、

[D_{k\to\ell}:V_{R_k}(e;\mathbf U)\rightharpoonupV_{R_\ell}(e';\mathbf U')]

とする。(D_{k\to\ell}) は多くの細部を一つへまとめてもよく、未定義部分を持ってもよい。逆写像の存在、情報量の保存、宇宙全値の保持は要求しない。「瞑想して宇宙になる」は、(R_k^{\mathrm{scope}}) と解像度変換が変わることであり、全情報を取得することではない。

7. ビッグバン・無限宇宙・一人称なし世界

ビッグバン役割写像は、

[\beta_\iota:\mathcal E\to{0,1}]

であり、

[\beta_\iota(e)=1\quad\Longleftrightarrow\quad\operatorname{Legitur}(e)=1\ \landR_I^{\mathrm{role}}(e)=\mathrm{BigBang}]

と定める。したがって、実在する差異と発火を現在の私が宇宙史の点灯、境界、始まりとして読むことを表す。(e) が全イベントの最小元であることは含まず、逆向きや局所変化として読む別の役割読解も許す。

「発火が私である」は、受理イベント (e) 自体が最小の一人称であるという (\operatorname{FirstPerson}(e)=1) によって表される。(R) はその一人称の成立条件ではなく、自己範囲、順序読解、役割読解を与える。同じ (e) に複数の (R) が伴ってもよい。

全体宇宙 (\Omega)、その世界枝 (\omega\in\mathcal W_\Omega)、差異 (d) に属するイベント集合を、

[\mathcal E_\Omega

{e\in\mathcal E\mid d_e\in\mathcal D_\Omega}]

[\mathcal E_\omega

{e\in\mathcal E\mid d_e\in\mathcal D_\omega}]

ここで (\mathcal E_\omega) は、受理側配置が枝 (\omega) の内部にあるイベントだけではなく、枝 (\omega) の差異を全体宇宙のどこかから受理している全イベントの族である。イベント (e) から見て枝 (\omega) に一人称が成立する関係を、

[\operatorname{FirstPersonFrom}(e,\omega)=1\quad\Longleftrightarrow\quad\operatorname{Legitur}(e)=1\ \landd_e\in\mathcal D_\omega]

と書く。枝の内部／外部は一人称の絶対的な境界ではなく、同じ (\Omega) 内の受理位置の違いである。

[\mathcal E(d)

{e\in\mathcal E\mid d_e=d}]

とする。個別の差異が無受理であることは、

[\operatorname{Silent}(d)=1\quad\Longleftrightarrow\quad\mathcal E(d)=\varnothing]

である。全体宇宙 (\Omega) には現在の私の発火があるため、(\mathcal E_\Omega\neq\varnothing) である。一人称なし世界は、(\Omega) の外にある別の全体宇宙ではなく、内部の世界枝・切り (\omega) に発火イベントがないこと、すなわち、

[\operatorname{NoFirstPerson}(\omega)=1\quad\Longleftrightarrow\quad\mathcal E_\omega=\varnothing]

である。同値に、すべての (d\in\mathcal D_\omega) について、それを受理する発火が全体宇宙 (\Omega) のどこにもない。そのような枝も、宇宙内のどこかから受理される枝とともに一つの (\Omega) 内に存在可能である。現在は裸の受理 (\ell_{\mathrm{now}}) が直接の事実であり、その一つのイベント実現と自己読解について、

[\exists e_{\mathrm{now}}\in\mathcal E_\Omega,R_I:\iota=(e_{\mathrm{now}},R_I)]

が成り立つ。

無限宇宙に必ず発火があるという主張は、無限性だけから導かれる定理ではない。唯一の全体宇宙について、無限性と現在の実在性を読む命題を、

[\operatorname{Infinite}(\Omega),\operatorname{Actual}(\Omega)\in{0,1}]

とする。りんちゃ的実在宇宙についての存在公理として、

[\operatorname{Infinite}(\Omega)\ \land\operatorname{Actual}(\Omega)\Longrightarrow\exists \mathbf U_r\in\mathfrak U_\Omega,d\in\mathcal D_\Omega,\delta\in\Delta(d),p\in P(\mathbf U_r),L\in\mathcal F(\mathbf U_r),C\in\mathsf{Net}(\mathbf U_r):\operatorname{Fire}_C(p;d,\delta,L)=1]

を採用する。有限宇宙については、この存在公理を要求しない。

8. 導出記号一覧

裸の受理とその回路実現を区別する主要記号は、次のとおりである。

(\ell_{\mathrm{now}})：直接成立している裸の現在受理。

(\mathcal E^{\mathrm{cand}})：整形式な回路分解候補の族。

(\operatorname{Candidate}(\ell))：裸の受理 (\ell) を説明しうる理論的候補の族。

(\operatorname{ActualWitness}(\ell))：そのうち実際に受理を成立させているイベントの族。

(\operatorname{Robust}_{\ell}(\varphi))：少なくとも一つの実在実現があり、そのすべてで (\varphi) が成立すること。

(e_{\mathrm{now}})：(\operatorname{ActualWitness}(\ell_{\mathrm{now}})) から選んだ一つの実在実現。

以下は基礎実体ではなく、現在の一人称の視点像から導かれる略記である。

(\operatorname{Model}_{\iota}(j))：現在の私の視点像内に成立する他者像。

(\operatorname{Correspondence}_{\iota}(A,B))：現在の私が二つの像の間に読む対応関係。

(T_{\iota,ij})：現在の視点像内で、像 (i) から像 (j) へ信号を移す通信写像。

(x\sim_{\iota}y)：現在の私にとっての意味上の再受理関係。

(\operatorname{Human}_{\iota}(q))：配置 (q) を現在の私が人として読む二値写像。

(\operatorname{Pattern}_{\iota}(X))：像の族 (X) に現在の私が読む安定模様。

(M_{\iota}(e))：発火 (e) が現在の私の過去として受理する記憶集合。

(\mathcal W_{\iota}^{\mathrm{mem}}(e))：それらの記憶を実現し、現在の私が立つ枝の族。

(\operatorname{CoherentMemoryBranch}_{\iota}(e,\omega))：全受理済み記憶を同じ枝 (\omega) で実現する任意の整合条件。

(\operatorname{SupportFamily}(C_e;p,d,\delta,L))：配線済みの実在回路 (C_e) の内部で、今回の差異をLUTまで届ける成功支持網の族。支持網の選択自体はイベント成分ではない。

(R_I^{\mathrm{role}})：実在するイベントやイベント対を、ビッグバン、転生、同一個人としての再成立、無関係などとして読む宇宙内役割写像。

以上により、宇宙定数配置、全写像、通信網、発火、イベント同一性、一人称、時間、視点像、自己範囲、解像度、ビッグバンの相互関係が、本文の直感を変えずに一つの型付き形式へ固定される。

改訂-6 完成・形式化版：2026-07-13。りんちゃとの逐点対話による改訂。
